# 我做了的事情

## 完成

1. **扩展 `.internal/`** — 新增更多 AI 可参照的参考文件，丰富上下文信息。
2. **整理 `change_log/`** — 梳理变更记录目录。
3. **工具栏 / 按钮组件 / Chrome 布局修复** — 复刻 v1.4.0 的部分组件，尽可能扩充可用组件数量，提升渲染覆盖度。
4. **更新根目录与 `.internal/`** — 所有文件内容均真实可信，与当前代码状态一致。
5. **数据中枢** - 新增数据中枢侧边栏。
6. **课程** - 着手复刻 v1.4.0 的课程栏目
7. **nav-button 组件 + 主页导航** — 新增 `nav-button` 组件类型，点击 emit `slot:switch_page:<target>` 实现页面跳转；新增 `collapsible` 参数控制 slot 折叠行为；showcase-dart 主页改为 3×3 网格导航。
8. **showcase WebView 空白修复** — `HtmlRenderView` 不再依赖 `showcase.exe` HTTP 端口，改为直接从 `v2ManifestProvider` 读取 manifest JSON 生成 HTML，与 Dart CompositeView 同源渲染，消除白页问题。

## 架构观察

昨天重构了插件的模块定义规范后，项目整体架构已基本收敛。后续新增功能组件等改动大概率不会触及核心骨架——但不排除后续发现架构漏洞的可能。

## 当前规划

- [ ] 全量插件复刻——确保下游组件尽可能覆盖常见使用场景
- [ ] 页面编辑技术（类似 PPT 编辑体验）——底层架构已支持，待实现

## 我在做的事情

1.跟踪数据流链路，诊断plugins\courses为何没法显示真实数据；

## 澄清

- **Python 解释器路径不存在**（`.greenix/python/python.exe` 未找到）：上轮交互中曾指出此问题，实际并非错误。内嵌 Python 体积过大、不适合直接 push；使用 `evg-base/scripts/` 内提供的脚本即可在本地快速重建 `python.exe`。