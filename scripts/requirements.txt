# Evergreen Multi-Tools — Python 脚本依赖
# 安装命令:
#   pip install -r requirements.txt
#
# 注意: 如果用于嵌入式 Python (scripts/python/)，请使用:
#   scripts/python/python.exe -m pip install -r scripts/requirements.txt

# ── OCR 脚本 (ocr_file.py, ocr_slides.py) ──
pytesseract>=0.3.10
Pillow>=10.0.0
requests>=2.28.0
pdf2image>=1.16.0

# ── PDF 翻译 (pdf_translate.py + pdf2zh_next) ──
BabelDOC>=0.6.0
PyMuPDF>=1.27.0
openai>=2.0.0
pydantic>=2.0.0
tomlkit>=0.12.0
rich>=13.0.0
peewee>=4.0.0
tenacity>=9.0.0
PyYAML>=6.0.0

# ── 测试依赖 ──
pytest>=7.0.0
