# M2 规划：展示类组件统一 dataSource 注入（Evergreen 渲染器数据改造）

> 创建：2026-07-11 ｜ 维护者：AI（常驻贡献者）｜ 跨轮次进度追踪文件
> 配套文档：`组件实现全景表.md`（§14.2 S1 / §14.3 M2 里程碑）、`M1规划.md`、`FAIL2CLONE.md`
> 源码根：`evg-base/lib/`（Dart 渲染 + 数据中枢）
> 副本隔离：所有改动仅限 `evg-base/`（副本目录），不碰正本 `evergreen-base\`

---

## 一、关键规则（本次改动必须严格遵守）

| # | 规则 | 说明 |
|---|------|------|
| R1 | 进度可追踪 | 本文件为唯一进度账本，跨多轮上下文须自解释；每轮结束更新状态列与统计 |
| R2 | 全链路接通 | 凡声明了 `dataSource` / `dataBindings` 的组件，必须**真正拉到数据并渲染**，禁止只在 descriptor 上留字段、运行时仍读静态 config |
| R3 | 必须有测试 | 解析器（dataPath / transform / endpoint 拉取）须有 `dart test`；每个改造组件至少有一个"注入数据后渲染"的 widget 测试 |
| R4 | 真实字段渲染 | 注入的数据必须落到组件已有 config 的真实字段（如 data-table 的 rows、chart 的 series），禁止另写一套硬编码展示 |
| R5 | 优雅降级 | 数据源拉取失败 / 超时 / 字段缺失时：保留上一次成功数据或空态提示，**绝不白屏 / 绝不崩溃** |
| R6 | 不碰 evg-base 红线 | 可改 `lib/renderer/` 与 `lib/core/data/`（M2 即数据改造）；但**不得改 core 模块范式契约**（`ModuleDescriptor` 字段已够用，仅扩展消费方） |
| R7 | 双端一致 | 凡 Dart 端支持的 dataSource 注入，HTML 端（`html_components.dart`）须有等价实现，零写死（沿用 R10 精神） |
| R8 | 自主完成不打扰 | 完成全部内容且自检全通过、无偷懒/逃避前**禁止停下**；**禁止打扰人类** |

**派生约束（来自项目既有铁律）：**
- 组件类型权威源 = `composite_view.dart:1157-1204` 的 `switch (config.type)`：**45 具名 + 20 placeholder = 全仓库 65 种**（M1 时 43 具名，后新增 `scraper-generator`、`tech-planner`）。
- 数据中枢 API 以 `lib/core/data/data.dart`（`DataOrchestrator` / `DataType` / `DataSourceDescriptor` / `DataBindingDescriptor`）为准。
- 展示类组件清单（S1 改造对象）固定为 §14.2 所列 12 种：`table` / `chart` / `calendar` / `timeline` / `timetable` / `card-list` / `kanban` / `tree` / `map` / `flashcards` / `quiz` / `stat-tile`。

---

## 二、当前仓库真实状态（本次读码审计基线）

> 下列结论均来自对 `evg-base` 副本的实际读码，非凭印象。**M2 的真实起点比 `组件实现全景表.md` §14.2 的描述更难**——基础设施在，但**端到端未接通**。

| 现状项 | 文件 / 行 | 真实情况 | 对 M2 的含义 |
|--------|-----------|---------|--------------|
| `DataSourceDescriptor` 定义 | `core/module/module_descriptor.dart:609` | 字段 `endpoint/method/dataPath/transform/refreshInterval` 已定义 + `fromJson` | 声明侧可用，无需改契约 |
| `DataBindingDescriptor` 定义 | `module_descriptor.dart:721` | `dataType/display/filter` 已定义 | 模块级绑定可用 |
| `ComponentDescriptor.dataSource` | `module_descriptor.dart:1297` | 已解析存储，但**全库无任何 slot 读取它** | 须在每个展示 slot 接通 |
| `ModuleDescriptor.dataBindings` | `module_descriptor.dart:2533` | 已解析存储 | 须接通（见下） |
| `DataOrchestrator` | `core/data/orchestrator.dart:95` | `register/get/refresh` + 缓存 + 状态 + 自动刷新，API 完整 | 运行时拉取中枢，直接复用 |
| 模块级 dataBindings 接通 | `renderer/page/composite_view.dart:325` | `DefaultView(descriptor: descriptor)` **未传 `tableData`**；全库无任何代码把 `dataBindings` 解析成 `tableData` 注入 | **当前模块级数据注入完全未接通**（DefaultView 永远空） |
| `data-table` slot | `renderer/components/data/data_table_slot.dart:31` | 只读 `config.config` 静态 `EditableTableData.fromJson`，**不碰 `dataSource`** | 须改为 ConsumerWidget + 注入 |
| `timetable` slot | `renderer/components/data/timetable_slot.dart:18` | `P4 迁移` 桩，渲染 "Timetable — P4 迁移" | 须先完成迁移（或 M2 内补基础实现）才能注入 |
| `dataPath` / `transform` | `module_descriptor.dart:616/620` | 仅解析存储，**全库 0 处应用**（搜索 `dataPath`/`transform` 仅命中解析与测试桩） | M2 必须新建提取 + 转换逻辑 |
| HTML 端 | `renderer/page/html_components.dart` | 对 `dataSource`/`dataBindings`/`DataOrchestrator` **零处理**（0 命中） | M2 须补 HTML 等价实现 |
| 注册桥（data 插件→orchestrator） | `main.dart:102 _scanAndRegisterDataSources` + `core/data/plugin/data_source_loader.dart` | 已能把 `plugins/*/data/manifest.json` 注册成 CLI/HTTP fetcher（`Process.run(exe,['--type',typeArg,'--project-root',root])`） | 模块级 `dataBindings.dataType` 可经此已注册 `DataType` 拉取 |
| Riverpod 入口 | `providers.dart` → `dataOrchestratorProvider` | `DataDashboardView` 已用 `ref.read(dataOrchestratorProvider)` 拉取 | slot 同样可经 `ref` 取 orchestrator |
| HTTP 客户端 | `pubspec.yaml` `dio ^5.9.2` | 已声明 | 组件级 `dataSource.endpoint` 直连拉取用 dio |

**基线小结**：M2 不是"把已有能力扩展到更多组件"，而是**从零接通**「声明(`dataSource`/`dataBindings`) → 注册/`DataOrchestrator` → 拉取 → `dataPath`/`transform` 处理 → 注入组件 config → 渲染 + 自动刷新」整条链路，且 Dart + HTML 双端都要做。

---

## 三、目标与范围

### 目标（对齐 §14.3 M2）
> **S1 统一 dataSource 注入落地**：所有 12 种展示类组件均可对接真实数据源，不再受限于静态 config；calendar/chart/stat-tile 等可拉取实时数据。

### 范围内
1. **数据解析器（新建）**：`resolveDataSource()` —— 统一处理 `endpoint` 直连拉取 / `dataBindings.dataType` 经 orchestrator 拉取，应用 `dataPath` 提取与 `transform` 转换。
2. **`dataPath` 提取工具（新建）**：点路径（如 `results.items`）walk JSON。
3. **`transform` 转换注册表（新建）**：命名函数映射（如 `toChart`/`toRows`/`toCalendar`/`toList`），内建 + 可扩展。
4. **组件级注入（改造 12 个展示 slot）**：改为 `ConsumerWidget`，有 `dataSource` 时拉取并合并进现有 config 字段。
5. **模块级 dataBindings 接通（改造 `CompositeView`）**：解析 `dataBindings` → 经 orchestrator 拉取 → 构建 `tableData` → 注入 `DefaultView`（修复当前空状态）。
6. **自动刷新**：尊重 `refreshInterval`；离开页面 ` Timer.dispose`。
7. **HTML 端等价实现（改造 `html_components.dart`）**：12 个展示组件的 `_renderX` 增加数据源解析与注入。

### 明确非目标（本里程碑不做）
- **M3 的 `pdf-viewer` / `scanner` 新组件**（见 §14.2 S2/S3）。
- **M4 的展示大厅插件**（S4）。
- **DesignerFlow 的可视化 `dataSource` 编辑 UI**（属 `规划A.md` 的 DesignerFlow / ConfigRegister，不在 M2 运行时改造内；但 M2 须保证 DesignerFlow 将来写出的 `dataSource` 字段能被本改造消费）。
- 修改 `ModuleDescriptor` 契约字段。

---

## 四、核心设计

### 4.1 数据解析器 `DataSourceResolver`

> 新建 `lib/renderer/data/data_source_resolver.dart`（纯 Dart，依赖 `data.dart` + `dio`）。

```dart
/// 解析一个 DataSourceDescriptor，返回已提取+转换后的数据（典型 List<Map> 或 Map）。
/// 两种来源：
///  1) endpoint 非空 → 用 dio 直连 HTTP 拉取（GET/POST 尊重 method）
///  2) 经 orchestrator：由调用方先 orch.get(DataType(name:...)) 拿到原始数据后传入
Future<dynamic> resolveDataSource({
  required DataSourceDescriptor ds,
  DataOrchestrator? orch,        // 模块级 dataType 路径用
  String? dataType,              // 对应已注册的 DataType.name（dataBindings 场景）
}) async {
  dynamic raw;
  if (ds.endpoint != null && ds.endpoint!.isNotEmpty) {
    raw = await _httpFetch(ds);                 // dio
  } else if (dataType != null && orch != null) {
    final t = DataType<dynamic>(name: dataType, category: '', displayName: dataType, ttl: Duration(minutes: 5));
    raw = await orch.get(t);                    // 已注册的 CLI/HTTP 插件数据源
  }
  if (raw == null) return null;
  if (ds.dataPath != null) raw = extractPath(raw, ds.dataPath!);  // 4.2
  if (ds.transform != null) raw = applyTransform(ds.transform!, raw); // 4.3
  return raw;
}
```

- **错误处理（R5）**：`try/catch` 包住拉取；失败时返回 `null`，调用方保留旧数据或空态，并 `Log().warn` 记录，不抛到 UI。
- **缓存**：`endpoint` 直连场景用 `dio` 自带 + 可选 `orch` 缓存；模块级 `dataType` 场景直接复用 `orch.get` 的缓存/状态。

### 4.2 `dataPath` 提取（新建 `lib/renderer/data/json_path.dart`）

```dart
/// 支持点路径与数组下标： 'a.b[0].c' → ((raw['a']??{})['b']??[])[0]['c']
dynamic extractPath(dynamic root, String path) {
  // 按 '.' 与 '[' 切分，逐段 walk；遇数组下标用 int 索引
}
```
> 不需要外部 JSONPath 库；自写 ~40 行覆盖 §14.2 示例（`results` / `results.items` 等）。

### 4.3 `transform` 转换注册表（新建 `lib/renderer/data/transform_registry.dart`）

```dart
typedef TransformFn = dynamic Function(dynamic raw);
final Map<String, TransformFn> transformRegistry = {
  'identity': (r) => r,
  'toList':   (r) => r is List ? r : (r is Map ? [r] : []),
  'toRows':   (r) => ...,   // Map→[{k:v}]，List 原样
  'toChart':  (r) => ...,   // 规整为 {series:[...], labels:[...]}
  'toCalendar': (r) => ..., // 规整为 events:[{date,title}]
  // 扩展点：插件可 registerTransform('myX', fn)
};
dynamic applyTransform(String name, dynamic raw) =>
    (transformRegistry[name] ?? transformRegistry['identity']!)(raw);
```
> 命名与 `descriptor_test.dart:444` 的 `transform: 'toChart'` 用例对齐，保证契约一致。

### 4.4 组件级注入模式（12 个展示 slot 统一改造）

每个展示 slot 由 `StatelessWidget` → `ConsumerWidget`（或 `ConsumerStatefulWidget`，如 data-table 需 `_controller`）：

```dart
class ChartSlot extends ConsumerWidget {
  final ComponentDescriptor config;
  const ChartSlot(this.config);
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final ds = config.dataSource;
    // 无 dataSource → 维持现有静态 config 渲染（向后兼容）
    if (ds == null) return _buildStatic(config.config);
    return FutureBuilder<dynamic>(
      future: resolveDataSource(ds: ds, orch: ref.read(dataOrchestratorProvider)),
      builder: (ctx, snap) {
        final merged = Map<String, dynamic>.from(config.config);
        if (snap.hasData) merged['chart'] = _mergeChart(snap.data); // 4.5 映射
        return _buildStatic(merged);
      },
    );
  }
}
```
> 关键点：**静态 config 仍生效**，有 `dataSource` 时以拉取数据覆盖对应字段 → 向后兼容（R4）。

### 4.5 组件级字段合并映射（S1 核心表）

| 组件 type | `dataSource` 拉取数据形态 | 合并进 config 的字段 | 备注 |
|-----------|--------------------------|----------------------|------|
| `data-table` | `List<Map>` | `rows`（列由 `_inferColumns` 或 config.columns 推断） | 须改 `DataTableSlot` 为 ConsumerStatefulWidget，`_controller` 用合并后数据重建 |
| `chart` | `{series,labels}` / `List<Map>` | `chart.data`（series/labels） | 复用现有 `ChartSlot` 的图表数据解析 |
| `calendar` | `{events:[{date,title,...}]}` | `events` | 替换静态 `config.events` |
| `timeline` | `List<Map>` | `items` | 替换 `config.items` |
| `timetable` | `{sessions:[...]}` | `sessions` | **先做 §4.6 的桩迁移** |
| `card-list` | `List<Map>` | `cards` | 替换 `config.cards` |
| `kanban` | `{columns:[{title,items}]}` | `columns` | 替换 `config.columns` |
| `tree` | `{root:{label,children}}` | `root` | 替换 `config.root` |
| `map` | `{center, zoom, markers}` | `map` | 替换 `config.map` |
| `flashcards` | `{wordList:[...], algorithm}` | `wordList` / `algorithm` | 替换 `config.wordList` |
| `quiz` | `{wordList, questionTypes, timeLimit, passScore}` | 对应字段 | 替换静态 |
| `stat-tile` | `{value, subtitle, trend}` / `Map` | `value`/`subtitle`/`trend` | 拉取动态指标 |

### 4.6 `timetable` 前置迁移

`timetable_slot.dart` 当前是 P4 桩，无法直接注入。M2 在 P2 首批**先完成 timetable 的基础迁移**（委托 `TimetableGrid` 渲染 `config.sessions`），再接入 dataSource。若迁移成本超预算，将其降级为 P3 末批并标注 FAIL2CLONE（R2）。

### 4.7 模块级 `dataBindings` 接通（修复当前空状态）

`CompositeView` 在构造 `DefaultView` 前，解析 `descriptor.dataBindings`：

```dart
// composite_view.dart 构造 DefaultView 处
if (descriptor.dataBindings.isNotEmpty) {
  final orch = ref.read(dataOrchestratorProvider);
  final tableData = <String, List<Map<String, dynamic>>>{};
  for (final b in descriptor.dataBindings) {
    try {
      final t = DataType<dynamic>(name: b.dataType, category: '', displayName: b.dataType, ttl: Duration(minutes: 5));
      final rows = await orch.get(t);          // 已注册 CLI/HTTP 插件数据源
      if (rows is List) tableData[b.dataType] = rows.cast<Map<String, dynamic>>();
    } catch (_) { /* 优雅降级：留空，DefaultView 渲染空态 */ }
  }
  return DefaultView(descriptor: descriptor, tableData: tableData);
}
```
> 这是把 §二 发现的"未接通"补上，使 `DefaultView` 的 `tableData` 契约真正生效（R2）。

### 4.8 HTML 端等价实现

`html_components.dart` 的 `_renderX` 在生成 HTML 字符串前，于 Dart 侧调用同一 `resolveDataSource`（Dart 已持有 orchestrator 与 dio），把解析后的数据**直接注入 HTML 模板**（与 Dart 端同理，不依赖 WebView 内 JS 拉取，避免跨域/时序问题）。每个展示组件 `_renderX` 增加 `dataSource` 分支，无 `dataSource` 时维持现有静态渲染（向后兼容）。R7 要求双端行为一致。

---

## 五、实现清单（P1–P3）

### P1 — 基础设施（4 天）
| # | 任务 | 产出物 | 验收 |
|---|------|--------|------|
| 1 | 数据解析器 `resolveDataSource` | `renderer/data/data_source_resolver.dart` | 单测：endpoint(dio mock) / dataType(orchestrator mock) 两路径返回正确 |
| 2 | `dataPath` 提取 | `renderer/data/json_path.dart` | 单测：`a.b[0].c` / 缺段返回 null 不崩 |
| 3 | `transform` 注册表 | `renderer/data/transform_registry.dart` | 单测：`toChart`/`toRows`/`toCalendar` 输出结构与 `descriptor_test` 契约一致 |
| 4 | `dio` 复用确认 + `dataOrchestratorProvider` 注入验证 | `pubspec`/providers 复核 | `flutter pub get` + 解析器能 `ref.read` 到 orchestrator |

> ## ⏱ 进度追踪（每轮更新）
> | 阶段 | 状态 | 说明 |
> |------|------|------|
> | P1 基础设施 | ✅ 完成 | `json_path`/`transform_registry`/`data_source_resolver` 三文件 + 单测 `test/renderer/data_test.dart` 14/14 通过（2026-07-11） |
> | P2 组件级注入（12 组件） | ✅ 完成 | **批次1**（stat-tile/chart/card-list/kanban/tree/timeline）8/8 + **批次2**（data-table/map/calendar/timetable 已完成 P4 迁移）4/4 + **批次3**（flashcards/quiz 内联 slot_widgets，因需 slotKey/moduleId/pageEventBus 不走 DataSourceSlot）4/4 → `data_slot_test.dart` 累计 **17/17** 通过（2026-07-11） |
> | P3-1 模块级 dataBindings 接通 | ✅ 完成 | `CompositeView` 转 ConsumerStatefulWidget，解析 `dataBindings` 经 orch 拉取 → 构建 `tableData` → 注入 `DefaultView`；`m2_module_binding_test.dart` **2/2** 通过（2026-07-11） |
> | P3-2 HTML 端 12 组件等价注入 | ✅ 完成 | 整条 HTML 管线改异步并贯穿 `DataOrchestrator? orch`；`html_components._resolveComp/_mergeIntoConfig` 按 §4.5 映射注入；`renderWithData(manifest, orch)` 新增；`m2_html_injection_test.dart` **3/3** 通过（2026-07-11） |
> | P3-3 自动刷新 | ✅ 完成 | 基类 `DataSourceSlotState` 加入 `refreshInterval` 周期 Timer + `dispose` 释放（覆盖 10 个继承 slot）；flashcards/quiz 独立 `_refreshTimer`（不与 quiz 倒计时冲突）；未开始/已结束则跳过，避免重置进度。**关键修正**：Timer 重抓走 `resolveDataSource(forceRefresh:true)` → `orch.refresh` 绕过缓存，否则 `orch.get` 命中缓存令刷新永远拿旧值；`data_slot_test` 新增「到点 forceRefresh 重抓并刷新 UI」断言（calls>1 证明二次拉取）。（2026-07-11） |
> | P3-4 端到端集成测试 | ✅ 完成 | 新增 `test/m2_data_source_test.dart`：组件级 `orch://` 注入 + 模块级 `dataBindings` 双链路同 orch 跑通 + HTML 端 `renderWithData` 一致性，**3/3** 通过（2026-07-11） |
>
> **汇总**：M2 全链路（声明→注册/orch→拉取→dataPath/transform→注入 config→渲染+自动刷新，Dart+HTML 双端）已落地；M2 专属 `dart test` 累计 **40/40** 通过（P1 14 + P2 17 + P3-1 2 + P3-2 3 + P3-3 1 + P3-4 3）。
> **注意**：`test/r10_render_log.dart` 为独立插件渲染校验台，其失败项（`ai-planner`/`scraper`/`showcase-v3`/`showcase-v3-html`）为工作区插件既有内容问题（缺失 `words.json`、含 `_supported` 外组件类型），与 M2 无关——`HtmlRenderer.render(manifest)` 传 `orch=null` 时 HTML 输出与改动前逐字节一致。
>
> **P2 实现要点**：新增注入基类 `renderer/data/data_source_slot.dart`（`DataSourceSlot`/`DataSourceSlotState`）统一处理 ConsumerState + Future 记忆化 + `mergeData` + 优雅降级；10 个 slot（data-table/chart/card-list/calendar/tree/timetable/timeline/stat-tile/map/kanban）继承之，覆写 `mergeData` 把拉取数据落入真实 config 字段；flashcards/quiz 因需 `slotKey/moduleId/pageEventBus` 无法继承基类，改为 `ConsumerStatefulWidget` + `_loadFromDataSource`（带 `_loadFromFile` 回退）。`resolveDataSource` 支持 `endpoint:'orch://<typeName>'` 引用已注册 DataType（不改动 `DataSourceDescriptor` 契约）。
>
> **P1 验证结论**：`resolveDataSource` 两路径（endpoint 直连 / orchestrator dataType）均返回正确数据；`dataPath`+`transform` 串联生效；拉取异常降级返回 `null` 不抛；`extractPath` 缺段/越界返回 `null` 不崩。无 mockito，测试用手写 fake（真实 `DataOrchestrator` + 注入 `httpFetcher`）。

### P2 — 组件级注入（12 天，含 timetable 迁移）
| # | 组件 | 端 | 当前 | 目标 | 状态 |
|---|------|----|------|------|------|
| 1 | data-table | D | 静态 config | ConsumerStatefulWidget + rows 注入 | ✅ |
| 2 | chart | D | 静态 config | ConsumerWidget + series 注入 | ✅ |
| 3 | stat-tile | D | 静态 config | value/subtitle/trend 注入 | ✅ |
| 4 | card-list | D | 静态 config | cards 注入 | ✅ |
| 5 | kanban | D | 静态 config | columns 注入 | ✅ |
| 6 | tree | D | 静态 config | root 注入 | ✅ |
| 7 | map | D | 静态 config | markers/center 注入 | ✅ |
| 8 | timeline | D | 静态 config | items 注入 | ✅ |
| 9 | flashcards | D | 静态 config | wordList 注入 | ✅ |
| 10 | quiz | D | 静态 config | 字段注入 | ✅ |
| 11 | timetable | D | **P4 桩** | **已完成迁移**（委托 TimetableGrid）+ sessions 注入 | ✅ |
| 12 | calendar | D | 静态 config | events 注入 | ✅ |
> 每完成一个：加一个"注入数据后渲染"widget 测试；`dart analyze` 零错误。

### P3 — 模块级接通 + HTML 端 + 刷新（9 天）
| # | 任务 | 产出物 | 验收 |
|---|------|--------|------|
| 1 | 模块级 dataBindings 接通 | `composite_view.dart` 改造 | ✅ DefaultView 不再恒空；传入 dataBindings 的模块显示真实数据 |
| 2 | HTML 端 12 组件等价注入 | `html_components.dart` | ✅ 每个 `_renderX` 增加 dataSource 分支；`m2_html_injection_test` 显示真实字段 |
| 3 | 自动刷新 | 各 slot `refreshInterval` Timer + dispose | ✅ 基类 + flashcards/quiz 独立 Timer，`dispose` 释放，无泄漏；Timer 重抓走 `forceRefresh` 绕过 orch 缓存（否则刷新无效） |
| 4 | 端到端集成测试 | `test/m2_data_source_test.dart` | ✅ 跑通 组件级 `orch://` endpoint + 模块级 dataBindings 两条链路（同 orch + HTML 一致性） |

**合计：约 25 工作日（P1 4 + P2 12 + P3 9），加联调/评审缓冲 → 现实 28–32 工作日。**

---

## 六、测试策略

| 层级 | 范围 | 命令 |
|------|------|------|
| 单元 | `extractPath` / `applyTransform` / `resolveDataSource`（mock dio + mock orch） | `flutter test test/renderer/data_test.dart` |
| Widget | 12 组件"注入数据后渲染"（断言合并字段出现在 Widget 树） | `flutter test test/renderer/data_slot_test.dart` |
| 集成 | 模块级 dataBindings 接通 + 端到端 endpoint 拉取 | `flutter test test/m2_data_source_test.dart` |
| HTML | `_renderX` 输出含真实字段、无写死 | 复用 R10 HTML 渲染日志自动检查 |

---

## 七、风险与对策

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| `data-table` 改 ConsumerStatefulWidget 后 `_controller` 重建时机错 | 中 | 中 | 仅当 `dataSource` 数据变化时 `setState` 重建 controller，静态场景保持原行为 |
| `timetable` 桩迁移超预算 | 中 | 中 | 降级为 P3 末批 + 写 FAIL2CLONE；其余 11 组件不阻塞 |
| `transform` 命名与既有契约不一致 | 低 | 高 | 注册表命名对齐 `descriptor_test.dart`（`toChart` 等），新增命名集中管理 |
| 组件级 endpoint 拉取阻塞 UI | 中 | 中 | `FutureBuilder` + 骨架屏/旧数据兜底；超时（dio `connectTimeout`）兜底空态 |
| HTML 端注入导致 WebView 重渲染闪烁 | 中 | 低 | 数据在 Dart 侧解析后整体注入模板，避免 WebView 内异步拉取 |
| `refreshInterval` Timer 泄漏 | 中 | 中 | 统一在 `dispose` 取消 Timer；ConsumerStatefulWidget 生命周期管理 |
| 模块级 dataBindings 拉取失败拖慢首屏 | 低 | 中 | 异步拉取 + 先渲染静态/空态，数据到达后 `setState` 刷新 |

---

## 八、里程碑对照（§14.3）

| 里程碑 | 目标 | 本规划对应 |
|--------|------|-----------|
| **M2: 改造** | S1 统一 dataSource 注入落地 | 本文件 P1–P3 全部完成 → 12 种展示组件均可对接真实数据源 |
| （前置）M1: 补齐 | 43→45 具名组件双端实现 | 已完成（见 `M1规划.md`），M2 在其上叠加数据能力 |
| （后续）M3: 扩展 | S2 `pdf-viewer` + S3 `scanner` | 组件类型 45→47（含 20 placeholder 则 65→67），不在本里程碑 |
| （后续）M4: 验收 | S4 展示大厅 | 依赖 M2/M3 完成 |

---

## 九、附录：本次审计涉及的权威文件清单

- 数据契约：`evg-base/lib/core/module/module_descriptor.dart`（`DataSourceDescriptor:609` / `DataBindingDescriptor:721` / `ComponentDescriptor.dataSource:1297` / `ModuleDescriptor.dataBindings:2533`）
- 数据中枢：`evg-base/lib/core/data/orchestrator.dart`（`get/refresh/缓存/状态`）、`data.dart`（barrel）、`plugin/data_source_loader.dart`
- 注册桥：`evg-base/lib/main.dart:102 _scanAndRegisterDataSources`
- 运行时消费（当前）：`evg-base/lib/renderer/page/composite_view.dart:325`（DefaultView 未传 tableData）、`renderer/page/data_dashboard_view.dart`（手动拉取）、`renderer/components/data/data_table_slot.dart`（静态）、`renderer/components/data/timetable_slot.dart`（桩）、`renderer/components/data/card_list_slot.dart`（DefaultView 契约）
- 依赖：`pubspec.yaml` `dio ^5.9.2`、`flutter_riverpod ^2.6.1`；Riverpod 入口 `providers.dart` → `dataOrchestratorProvider`
- HTML 端：`evg-base/lib/renderer/page/html_components.dart`（待补 dataSource 处理）

> 评审待办（交由架构/总工 subagent 复核）：
> - [ ] `resolveDataSource` 的两条来源（endpoint 直连 / orchestrator dataType）是否与现有注册桥（`_scanAndRegisterDataSources` + `DataSourceLoader`）在命名与生命周期上一致，无重复注册冲突。
> - [ ] 12 组件字段合并映射是否与各 slot 现有 config 真实字段逐一对齐（R4），无遗漏组件。
> - [ ] HTML 端实现是否与 Dart 端行为一致（R7），R10 日志检查可验证真实字段。
