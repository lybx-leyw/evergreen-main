# 三模式视图重构 · 实施计划（v2.0）

> 状态：**方案已与用户确认（2026-08）**，实施中。
> 目标：App 壳层支持三种视图模式，重构侧边栏导航体系。

## 一、需求

1. **AI 视图**：AI 助手为主视图；无左侧展开栏；左侧窄轨只有 4 个按钮——
   显示设置、插件中心、数据中心、远程同步（占位），点击弹出对应模块。
2. **开发者模式**：展示主题创作、插件制作、数据爬取三个插件；无左侧展开栏；
   左栏同为 4 个系统按钮；**安卓版撤销数据爬取支持**——占位并提示使用 Windows 版。
3. **插件视图**：左侧栏展示除 AI 助手、主题创作、插件制作、数据爬取外的
   其他所有插件，UI 与现在一致。

## 二、插件术语映射（manifest 实况）

| 术语 | 插件 id | 现名 | 路由 |
|---|---|---|---|
| AI 助手 | ai-assistant | AI 助手 | /ai-assistant |
| 主题创作 | theme-creator | 主题创作中心 | /theme-creator |
| 插件制作 | html-creator | HTML 插件创作中心 | /html-creator |
| 数据爬取 | scraper | 所见即所得爬虫 | /scraper |
| 显示设置 | settings | 设置 | /settings |
| 插件中心 | marketplace | 插件市场 | /marketplace |
| 数据中心 | data-dashboard | 数据中枢 | /data-dashboard |
| 远程同步 | （无） | 占位 | — |

## 三、已确认决策

1. **模式切换**：左栏顶部视图图标，点击弹出**扇形菜单**（AI 视图 / 开发者模式 /
   插件视图），点击即切换。
2. **弹出对应模块**：全屏推入新页 + 返回按钮；AI 会话在 provider 中后台继续。
3. **开发者模式布局**：左栏 4 系统按钮下方再放 3 个插件图标，点击切换主区，
   IndexedStack 保持各插件状态。
4. **安卓爬取**：插件视图本就不显示 4 个特殊插件；开发者模式爬取图标变占位，
   点击提示「安卓不支持，请使用 Windows 版」。
5. **模式 3 侧栏**：保留设置/插件市场/数据中枢，UI 与现在完全一致；
   4 按钮窄轨只出现在模式 1/2。
6. **路由隔离**：不硬阻断；深层链接（市场卡片/命令面板/URL）照常可用。
7. **模式 3 首页**：'/' 自动跳转到侧栏第一个插件。

## 四、补充提案（已确认）

- a. 默认 **AI 视图**；切换后 SharedPreferences 持久化（key: app_mode）。
- b. 远程同步：占位样式，点击提示「远程同步即将上线」。
- c. 命名：窄轨按钮用「显示设置/插件中心/数据中心」；模式 3 侧栏保持
   「设置/插件市场/数据中枢」原名。
- d. 开发者模式主区默认「主题创作」，会话内记住上次选择。
- e. '/' 按模式重定向：AI → /ai-assistant；开发者 → /dev-hub；插件 → 侧栏第一项。
- f. 移动端：模式 1/2 用同一窄轨（图标版 + 扇形切换）；模式 3 保持现有
   Drawer + 底部导航。
- g. 推入页返回：主区左上角浮动返回按钮（canPop 时出现），颜色从
   Theme.colorScheme 派生。
- h. 扇形菜单：自绘轻量 Overlay（约 90° 弧线 + 交错动画），不引第三方包。

## 五、实现拆分

**新增**
1. `lib/renderer/app/app_mode.dart` — AppMode 枚举 + appModeProvider（持久化）
   + 特殊插件 id 集合 + 导航过滤（模式维度）。
2. `lib/renderer/app/mode_rail.dart` — 窄轨：视图图标 + 扇形菜单、4 系统按钮、
   开发者模式 3 插件图标（安卓爬取占位分支）。
3. `lib/renderer/app/dev_mode_hub.dart` — 模式 2 主区：IndexedStack 三插件页
   + 安卓爬取占位页。
4. 测试：`test/renderer/app_mode_test.dart`、`mode_rail_test.dart`、
   `dev_mode_hub_test.dart`（实跑由用户本地执行）。

**修改**
5. `app_shell.dart` — 按模式渲染壳层；模式 1/2 窄轨 + hub，模式 3 现有侧栏；
   浮动返回按钮。
6. `app.dart` — '/' 按模式重定向；新增 /dev-hub 路由。
7. 导航过滤与模式逻辑组合（复用现有 filterNavByPluginState）。

**不动**：ModuleRegistry、插件 manifest、ModuleDispatch、插件内部代码。

## 六、验证

- 容器无法跑 flutter test/analyze（用户指示本轮不跑）；新增测试由用户本地回归。
- 重点回归：test/module/marketplace_*、module_dispatch_test、widget_test。
- 通过后按仓库规则开 PR 到受保护 v2.0（推送需用户新 PAT）。

---

## 七、实施进度（随做随记）

### Phase 1 · 三模式视图重构 —— ✅ 已完成

| 文件 | 动作 | 说明 |
|---|---|---|
| lib/renderer/app/app_mode.dart | 新增 | AppMode 枚举、appModeProvider、devHubIndexProvider、kSpecialPluginIds、kDevPluginIds、filterNavByAppMode/Flat、defaultRouteForMode、setAppMode |
| lib/renderer/app/mode_rail.dart | 新增 | 窄轨 + 视图图标扇形菜单（Overlay + 交错动画）+ 4 系统按钮 + 开发者 3 插件入口（安卓爬取占位） |
| lib/renderer/app/dev_mode_hub.dart | 新增 | IndexedStack 三插件 + 安卓爬取占位页 + 插件未安装占位 |
| lib/renderer/app/app_shell.dart | 修改 | AppShell 按模式渲染；_RailShell + 浮动返回按钮；模式 3 侧栏/Drawer/底部导航四处置入 filterNavByAppMode |
| lib/app.dart | 修改 | '/' 按模式 redirect + /dev-hub 路由 + 模式切换重建 GoRouter |
| lib/app_bootstrap.dart | 修改 | ProviderScope 注入时 override appModeProvider（上次选择持久化，无闪动） |
| test/renderer/app_mode_test.dart / mode_rail_test.dart / dev_mode_hub_test.dart | 新增 | 纯逻辑 + widget 测试（未实跑，用户本地回归） |

验证：容器 dart analyze 对以上文件 0 error / 0 warning（仅 info 级风格提示，与仓库既有风格一致）。
flutter test / flutter analyze 按用户指示未跑。

### Phase 2 · 创作断点续作 + 画板-数据源绑定 —— 🔨 进行中

**A. scraper 断点续作 —— ✅ 已完成**
- A1 ScraperWorkflow：toJson 补全（pythonOutput/pendingTerminal/terminalResult/warningSent3/awaitingUserConfirm/logSeq/phaseEnteredAt）+ restoreFromJson
- A2 ExploreWorkflow：toJson / restoreFromJson
- A3 ScraperGeneratorView：画板状态落盘（boards/<id>/workflow.json 载荷含 dataName/pluginDir、explore_workflow.json）+ 防抖保存 + 恢复（非 idle 不自动 startCapturing）+ resume_prompt.txt / bound_sources.json 读取 + 面板新参数透传
- A4 ScraperAIPanel：会话按画板沙盒（boards/<id>/session.json，旧文件自动迁移）+ Agent 上下文侧车 agent_session_<idx>.json（不再整体丢弃，只截 >50KB 工具结果）+ 无快照时从 UI 消息重建上下文 + 系统提示追加断点续作状态块（_skillBodyWithResume/_resumeContext）+ startExplore/triggerAnalyze 续作分支 + _maybeSendResumePrompt + 恢复气泡

**B. 主题创作历史 —— ✅ 已完成**
- B1 新增 services/theme_chat_store.dart（workspace 单会话、24 条上限）；ThemeAiService.generate(history)；视图接线（携带历史 + 成功回写）

**C. HTML 创作 —— ⬜ 未开始**
- C1 html_ai_service._restoreCanvasSession：恢复时给 system prompt 追加断点续作说明

**D. 画板-数据源绑定 —— ⬜ 未开始（用户需求全文见根目录交接文档）**
- D1 _buildExploreSource/_generatePlugin 产物 manifest 写入 boardId/createdBy 溯源；建板回写 boundBoardId
- D2 新增 board/data_source_binding.dart（扫描 plugins/*/data/manifest.json + 截断 JSON 输出）
- D3 BoardContainerView 左栏树状（画板→数据源）+ 数据源视图切换 + JSON 弹出 + 「建立画板？」弹窗 + 已有画板直入
- D4 数据源建板：拷贝脚本/JSON 到 boards/<id>/source/、workflow.json 置 debugging + pythonCode、resume_prompt.txt、bound_sources.json
- GeneratorView 兜底：bound_sources.json 缺失时按 boardId 动态扫描绑定源（AI 告知数据状态）

**E. 计划文档进度 —— ✅ 本次更新**
**F. 本地 commit —— ⬜ 未提交（未 push，v2.0 受保护走 PR）**
