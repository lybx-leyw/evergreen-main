# M1 规划：组件全量补齐

> 创建：2026-07-08 | 配套：`组件实现全景表.md` | 目标：43 个具名组件 Dart + HTML 双端全量 ✅，零硬编码
> 基线：Dart 12 ❌ + HTML 7 ❌(全硬编码) + 11 ⚠️(部分硬编码) + data-dashboard HTML 未注册

---

## 一、任务总览

```
M1 总任务 = Dart 12 组件落地 + HTML 19 组件升级
         ├── P1 速赢（4 项）：已有原子组件或实现极简，可直接收口
         ├── P2 标准（13 项）：需一定工作量，但逻辑清晰无阻塞
         └── P3 攻坚（14 项）：需较大重构或全新实现，依赖清晰
```

---

## 二、P1 速赢（预计 4 项，优先完成）

> 特点：widgets/ 已有现成组件只需接入 SlotDispatch + switch，或 HTML 改动极小。

| # | 组件 | 端 | 当前 | 目标 | 方案 |
|---|------|----|------|------|------|
| P1-1 | `audio-player` | Dart | ❌ UnknownSlot | ✅ AudioPlayerSlot | `widgets/audio_player.dart` 已有 `AudioPlayerWidget`，创建 `shared/_audio_player_slot.dart`，读取 `config.src`/`config.title` 委托渲染，SlotDispatch 新增 case |
| P1-2 | `image-gallery` | Dart | ❌ UnknownSlot | ✅ ImageGallerySlot | `widgets/image_viewer.dart` 已有 `ImageViewer`，创建 `shared/_image_gallery_slot.dart`，读取 `config.images[]` 渲染网格，SlotDispatch 新增 case |
| P1-3 | `notepad` | Dart | ❌ UnknownSlot | ✅ NotepadSlot | 创建 `shared/_notepad_slot.dart`：读取 `config.content`/`config.placeholder`，渲染标题 + 可编辑文本区（`TextField` 多行），SlotDispatch 新增 case |
| P1-4 | `data-dashboard` | HTML | ❌ 未注册 | ✅ 注册渲染 | 在 `_renderers` map 新增 `'data-dashboard': _renderDataDashboard`，复用 dataBindings 渲染仪表盘卡片网格 |

---

## 三、P2 标准（预计 13 项，逐个推进）

> 特点：逻辑明确但需一定实现量，无外部依赖阻塞。

### 3.1 Dart 新增 Slot 实现（6 项）

| # | 组件 | 当前 | 方案 |
|---|------|------|------|
| P2-1 | `prompt-builder` | ❌ UnknownSlot | 创建 `shared/_prompt_builder_slot.dart`：读取 `config.template`（带 `{{var}}` 占位符）+ `config.variables{}`（变量名→默认值），渲染模板预览 + 变量编辑表单 + 生成按钮。可复用 form 渲染字段。 |
| P2-2 | `tree` | ❌ UnknownSlot | 创建 `shared/_tree_slot.dart`：读取 `config.root{label, children[], icon}`，递归渲染 `ExpansionTile` 嵌套列表，支持展开/折叠。palace 模块依赖此组件。 |
| P2-3 | `webview` | ❌ UnknownSlot | 创建 `shared/_webview_slot.dart`：读取 `config.url`，优先尝试 `webview_flutter`（若可用），否则渲染带 URL 的占位卡片（用户可复制到浏览器）。`config.allowScripts` 控制 JS 开关。 |
| P2-4 | `custom` | ❌ UnknownSlot | 创建 `shared/_custom_slot.dart`：读取 `config.html`（显示安全警告 + 原始文本预览）或 `config.src`（iframe 模式），安全考虑不直接 inject HTML 到 Flutter widget tree。 |
| P2-5 | `diff-viewer` | ❌ UnknownSlot | 创建 `shared/_diff_viewer_slot.dart`：读取 `config.leftLabel`/`config.rightLabel`/`config.lines[].type/text`，渲染双栏对比视图（左旧右新），行按 type(`add`/`del`/`same`) 着色。 |
| P2-6 | `terminal` | ❌ UnknownSlot | 创建 `shared/_terminal_slot.dart`：读取 `config.cwd`/`config.lines[].prompt/text/color`，渲染深色终端模拟器（只读模式），支持命令行样式输出。后续可扩展对接真实后端进程。 |

### 3.2 HTML 硬编码→动态（7 项）

| # | 组件 | 当前 | 方案 |
|---|------|------|------|
| P2-7 | `divider` | ❌ 永远 `<hr>` | 读 `config.style`（dashed/dotted/solid）、`config.margin`、`config.indent`，渲染对应 CSS 样式。若无 config 则保持默认 `<hr>`。 |
| P2-8 | `code-editor` | ⚠️ language 动态，代码硬编码 | 优先读 `config.content`（完整代码字符串），为空时读 `config.sample` → 选 `_codeSamples` 对应语言示例。不再无条件硬编码。 |
| P2-9 | `document` | ⚠️ exportFormats 动态，内容硬编码 | 读取 `config.content`（Markdown/纯文本）或 `config.paragraphs[]`，动态渲染文档内容，格式标签从 `exportFormats` 读取。 |
| P2-10 | `lottery-wheel` | ⚠️ title 动态，段硬编码 | 读取 `config.segments[]`（label + weight），动态生成转盘扇区和抽奖逻辑。空时显示优雅空态。 |
| P2-11 | `kanban` | ⚠️ columns 动态，空时硬编码 | 移除 `_renderKanban` 中的默认 3 列硬编码（待办/进行中/已完成）。`config.columns` 为空时渲染空态提示："配置 columns 来创建看板"。 |
| P2-12 | `tree` | ⚠️ root 动态，null 时硬编码 | 移除 `_renderTree` 中的默认 3 级树硬编码。`config.root` 为 null 时渲染空态提示。 |
| P2-13 | `map` | ⚠️ center 动态，无真实地图 | 嵌入 Leaflet.js（CDN）+ OpenStreetMap tile，读取 `config.map.center.lat/lng`、`config.map.zoom`、`config.map.markers[]`，渲染可交互地图。若 CDN 不可用则回退到当前占位符。 |

---

## 四、P3 攻坚（预计 14 项，需较大重构）

> 特点：当前全硬编码需完全重写，或需整合外部库/复杂交互。

### 4.1 HTML 全硬编码重写（6 项）

| # | 组件 | 当前 | 方案 |
|---|------|------|------|
| P3-1 | `spreadsheet` | ❌ 5×5 固定表格 | 读取 `config.columns[]`（列定义 + 数据类型）+ `config.rows[][]`（二维数据），渲染动态表格，支持列宽调整、选中高亮。首次实现不做公式计算（后续扩展）。 |
| P3-2 | `presentation` | ❌ 「幻灯片演示 1/5」 | 读取 `config.slides[]`（每页含 title/body/image/layout），渲染幻灯片容器 + 翻页按钮。首页→末页循环。 |
| P3-3 | `type-check` | ❌ 固定 Python 题目 | 读取 `config.wordList[]`（打字练习词表）或 `config.question`/`config.options[]`/`config.answer`，渲染打字输入框 + 正确/错误反馈 + 实时 WPM/准确率统计。 |
| P3-4 | `calendar` | ❌ 2026-07 硬编码 | 读取 `config.events[]`（date/title/color/description），渲染可翻月月历网格，有事件日期打色点，点击日期显示当日事件列表。默认显示当前月份。 |
| P3-5 | `mindmap` | ❌ 「核心主题」 | 读取 `config.nodes[]`（label/children[]），使用 SVG/Canvas 自绘树形导图：根节点居中，子节点径向展开，连线。首次可用简单 CSS 树形替代（如 `_renderTree` 结构 + CSS indented tree）。 |
| P3-6 | `whiteboard` | ❌ 读 cfg 但丢弃 | 读取 `config` 的工具栏（画笔/橡皮擦/形状/颜色/线宽）、画布尺寸、背景色，渲染 Canvas + 工具栏 UI。首次实现基础画笔涂鸦即可。 |

### 4.2 HTML 部分硬编码深改（5 项）

| # | 组件 | 当前 | 方案 |
|---|------|------|------|
| P3-7 | `chart` | ⚠️ type/title/legend 动态，数据全硬编码 | 读取 `config.chart.data[]`（系列数据：labels + datasets[].values/color），动态注入到 bar/pie/line 渲染函数。替换当前硬编码数值。空数据时渲染空态。 |
| P3-8 | `diff-viewer` | ⚠️ label 动态，diff 行硬编码 | 读取 `config.left`/`config.right`（两端完整文本），在前端执行简易逐行 diff 算法（LCS 或逐行对比），动态渲染差异结果。空时渲染空态。 |
| P3-9 | `terminal` | ⚠️ cwd 动态，输出硬编码 | 读取 `config.lines[]`（prompt + text + color），动态渲染终端行。若 `config.lines` 为空则渲染 `cwd` 提示符 + 闪烁光标（只读模式）。 |
| P3-10 | `crossword` | ⚠️ title 动态，网格硬编码 | 读取 `config.grid[][]`（二维字符数组，空字符串=黑格）+ `config.clues.across[]`/`config.clues.down[]`（提示语），渲染填字游戏网格 + 输入交互。 |
| P3-11 | `pronunciation` | ⚠️ word/phonetic 动态，分数硬编码 | 读取 `config.word`/`config.phonetic`，移除硬编码 85/100 分数。若 `config.score` 有值则显示分数，否则显示「未评测」状态 + 录音按钮（Web Speech API）。 |

### 4.3 Dart 全新实现（3 项）

| # | 组件 | 当前 | 方案 |
|---|------|------|------|
| P3-12 | `whiteboard` | ❌ UnknownSlot | 创建 `shared/_whiteboard_slot.dart`：使用 `CustomPaint` + `GestureDetector` 实现基础画笔涂鸦。读取 `config.tools[]`（pen/eraser/shape）、`config.colors[]`、`config.lineWidth`。首次只做单色画笔 + 清除按钮。 |
| P3-13 | `crossword` | ❌ UnknownSlot | 创建 `shared/_crossword_slot.dart`：读取 `config.grid[][]` + `config.clues`，使用 `GridView` 渲染填字网格 + 输入焦点管理 + 方向切换（横/纵）+ 提示语面板。 |
| P3-14 | `pronunciation` | ❌ UnknownSlot | 创建 `shared/_pronunciation_slot.dart`：读取 `config.word`/`config.phonetic`，渲染单词卡 + 音标 + 发音按钮（AudioPlayer）+ 录音按钮（需录音权限）+ 评分显示（如有）。首次可先做静态展示，录音评分留接口。 |

---

## 五、执行依赖图

```
P1 速赢（无依赖，可直接并行）
├── P1-1 audio-player Dart    ← widgets/audio_player.dart 已存在
├── P1-2 image-gallery Dart   ← widgets/image_viewer.dart 已存在
├── P1-3 notepad Dart         ← 纯新文件，无依赖
└── P1-4 data-dashboard HTML  ← 纯注册，无依赖

P2 标准（P1 完成后可并行推进）
├── P2-1~P2-6  Dart 新增 Slot  ← 各自独立，可并行
└── P2-7~P2-13 HTML 动态化    ← 各自独立，可并行

P3 攻坚（P2 完成后推进，部分需顺序）
├── P3-1 spreadsheet HTML     ← 独立
├── P3-2 presentation HTML    ← 独立
├── P3-3 type-check HTML      ← 独立
├── P3-4 calendar HTML        ← 独立
├── P3-5 mindmap HTML         ← 可复用 P2-12 tree HTML 的 CSS 缩进模式
├── P3-6 whiteboard HTML      ← 独立
├── P3-7 chart HTML           ← 需等 S1 dataSource 机制（可先做静态数据改造）
├── P3-8 diff-viewer HTML     ← 独立
├── P3-9 terminal HTML        ← 可参考 P2-6 terminal Dart
├── P3-10 crossword HTML      ← 可参考 P3-13 crossword Dart
├── P3-11 pronunciation HTML  ← 可参考 P3-14 pronunciation Dart
├── P3-12 whiteboard Dart     ← 独立
├── P3-13 crossword Dart      ← 独立
└── P3-14 pronunciation Dart  ← 独立
```

---

## 六、验收标准

每完成一个组件，需同时满足：

| 检查项 | 说明 |
|--------|------|
| ✅ SlotDispatch 注册 | `switch (config.type)` 新增正确 case，不再是 `_UnknownSlot` |
| ✅ HTML 注册 | `_renderers` map 有对应渲染函数（或合理别名） |
| ✅ config 字段读取 | 所有展示数据来自 `config.*`，零硬编码 |
| ✅ 空态优雅 | `config` 为空/缺失时显示提示而非白屏或崩溃 |
| ✅ Dart analyze 通过 | `cd evg-base && dart analyze lib/renderer/` 零错误 |
| ✅ R10 HTML 日志 | `flutter test test/r10_render_log.dart` 真实数据通过 |
| ✅ 全景表更新 | `组件实现全景表.md` 对应行状态 ✅ |

---

## 七、文件变更清单

### 7.1 需新增文件

```
evg-base/lib/renderer/shared/
├── _audio_player_slot.dart      # P1-1
├── _image_gallery_slot.dart     # P1-2
├── _notepad_slot.dart           # P1-3
├── _prompt_builder_slot.dart    # P2-1
├── _tree_slot.dart              # P2-2
├── _webview_slot.dart           # P2-3
├── _custom_slot.dart            # P2-4
├── _diff_viewer_slot.dart       # P2-5
├── _terminal_slot.dart          # P2-6
├── _whiteboard_slot.dart        # P3-12
├── _crossword_slot.dart         # P3-13
└── _pronunciation_slot.dart     # P3-14
```

### 7.2 需修改文件

```
evg-base/lib/renderer/shared/composite_view.dart
    ← SlotDispatch switch + imports 新增 12 个 case

evg-base/lib/renderer/html/html_components.dart
    ← 19 个 _render* 函数改造（P2-7~P3-11）
    ← _renderers map 新增 data-dashboard
```

---

## 八、进度追踪

| 批次 | 范围 | 项数 | 状态 | 完成日期 |
|------|------|------|------|---------|
| P1 速赢 | audio-player(Dart) + image-gallery(Dart) + notepad(Dart) + data-dashboard(HTML) | 4 | ⚪ 待启动 | — |
| P2 标准 | prompt-builder/tree/webview/custom/diff-viewer/terminal(Dart) + divider/code-editor/document/lottery-wheel/kanban/tree/map(HTML) | 13 | ⚪ 待启动 | — |
| P3 攻坚 | spreadsheet/presentation/type-check/calendar/mindmap/whiteboard/chart/diff-viewer/terminal/crossword/pronunciation(HTML) + whiteboard/crossword/pronunciation(Dart) | 14 | ⚪ 待启动 | — |
| **合计** | | **31** | | |

> 进度符号：⚪ 待启动 / 🔧 进行中 / ✅ 完成 / ⛔ 阻塞
