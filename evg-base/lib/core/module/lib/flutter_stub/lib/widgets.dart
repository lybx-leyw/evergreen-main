/// Flutter widgets stub — 仅提供 [IconData]。
///
/// 实际使用时由主项目的 Flutter SDK 覆盖。

/// 图标标识（codepoint + 可选字体族）。
class IconData {
  final int codePoint;
  final String? fontFamily;

  const IconData(this.codePoint, {this.fontFamily});
}
