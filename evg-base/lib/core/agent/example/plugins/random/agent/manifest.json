{
  "name": "random",
  "description": "生成随机整数。",
  "schema": {
    "type": "object",
    "properties": {
      "min": { "type": "integer", "description": "最小值，默认 1" },
      "max": { "type": "integer", "description": "最大值，默认 100" }
    },
    "required": []
  },
  "readOnly": true,
  "argMode": "args",
  "argSpec": {
    "style": "flag",
    "prefix": "--"
  }
}
