{
  "name": "time",
  "description": "获取当前时间，支持指定时区偏移。",
  "schema": {
    "type": "object",
    "properties": {
      "offset": { "type": "integer", "description": "时区偏移（小时），默认 8" },
      "format": { "type": "string", "description": "12h 或 24h，默认 24h", "enum": ["12h", "24h"] }
    },
    "required": []
  },
  "readOnly": true,
  "runtime": "python",
  "argMode": "args",
  "argSpec": {
    "style": "flag",
    "prefix": "--"
  }
}
