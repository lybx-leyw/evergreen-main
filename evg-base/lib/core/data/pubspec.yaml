name: evergreen_base
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  path: ^1.8.0
  meta: ^1.9.0
  path_provider:
    path: lib/path_provider_stub

dev_dependencies:
  test: ^1.24.0
