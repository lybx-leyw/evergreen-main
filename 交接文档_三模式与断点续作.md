# 交接文档 · 三模式视图重构 + 创作断点续作 + 画板-数据源深度绑定

> 状态：Phase 1 完成；Phase 2 A/B/C1 完成（C1 已于当前会话落地），D1–D4 待做。
> 未 commit、未 push、未实跑测试。本会话所有改动都在工作树，待验证后提交。
> v2.0 受保护，最终走 PR。

---

## 一、本会话做了什么

### Phase 1 · App 壳层三模式视图（已完成）

- 新增 lib/renderer/app/app_mode.dart（AppMode 枚举 + provider + 导航过滤 + '/' 重定向）
- 新增 lib/renderer/app/mode_rail.dart（窄轨 + 视图图标扇形菜单三模式切换 + 4 系统按钮 + 开发者 3 插件入口，安卓爬取占位）
- 新增 lib/renderer/app/dev_mode_hub.dart（IndexedStack 三插件 + 安卓爬取占位页）
- 修改 app_shell.dart（模式 1/2 窄轨壳层 + 浮动返回；模式 3 保持原侧栏并排除 4 个特殊插件）
- 修改 app.dart（'/' 按模式 redirect、/dev-hub 路由、模式切换重建 GoRouter）
- 修改 app_bootstrap.dart（注入时 override appModeProvider，持久化上次模式）
- 新增测试 test/renderer/app_mode_test.dart / mode_rail_test.dart / dev_mode_hub_test.dart（未实跑）
- 容器内 dart analyze 对 Phase 1 文件：0 error / 0 warning（仅 info 级风格提示）

### Phase 2A · scraper 断点续作（已完成，未编译验证）

- workflow/scraper_workflow.dart：toJson 补全 + restoreFromJson
- explore/explore_workflow.dart：新增 toJson / restoreFromJson
- view/scraper_generator_view.dart：画板状态落盘/恢复/防抖保存；
  boards/<id>/workflow.json（载荷 {workflow, dataName, pluginDir}）、explore_workflow.json、
  resume_prompt.txt、bound_sources.json；恢复出非 idle 不再自动 startCapturing；
  向面板传 resumeDataName/resumePluginDir/resumePrompt/boundSourcesJson
- agent/scraper_ai_panel.dart：
  1) 会话按画板沙盒 boards/<id>/session.json（旧 scraper_sessions.json 自动迁移）
  2) Agent 上下文侧车 agent_session_<idx>.json（修复 1MB 整体丢弃导致历史不完整；
     只截 >50KB 的单条工具结果）
  3) 无快照时从 UI 消息重建 Agent 上下文（修复「传给 AI 的对话历史不完整」）
  4) 系统提示 = Skill + 断点续作状态块（_skillBodyWithResume/_resumeContext，
     告知已完成步骤、禁止重做 → 修复「skill 重发掩盖已完成 step」）
  5) startExplore/triggerAnalyze 恢复分支（非 idle 不再重发全新任务 prompt）
  6) _maybeSendResumePrompt（数据源建板一次性续作 prompt）+ 恢复气泡

### Phase 2B · 主题创作历史（已完成，未编译验证）

- 新增 theme_creator_modle/services/theme_chat_store.dart（workspace 单会话 chat.json，24 条上限）
- theme_ai_service.dart：generate(description, {history})
- theme_creator_view.dart：生成前带历史、成功后回写本轮（指令 + 草稿 JSON 摘要）

---

## 二、未完成清单（严格按序做）

### C1 · HTML 创作断点续作说明 ✅（已完成，未编译验证）

文件 lib/renderer/templates/v4_modle/components/creative/html-creator/services/html_ai_service.dart
_restoreCanvasSession() 中 _assembly.controller.setSystemPrompt(_systemPrompt) 已改为：
恢复的消息数 > 0 时在 system prompt 末尾追加——

    ## 断点续作
    上次会话已恢复（N 条消息）。若上一次任务未完成，请直接继续执行，
    不要从头解释或重做已完成的修改。

（N = restored.messages.length；消息数为 0 时保持原 system prompt 不变。）

### D1 · 数据源产物溯源（先做，D3 依赖）

文件 scraper_modle/agent/scraper_ai_panel.dart
- 新增 _patchManifestProvenance(pluginDir, {boardId, createdBy})：
  读 plugins/data-<name>/data/manifest.json，写顶层 boardId（创建画板）与
  createdBy（'scraper-explore' / 'scraper-capture'），失败静默。
- 调用点 1：_buildExploreSource 在 generateAsDataPlugin 成功后 →
  createdBy 'scraper-explore'，boardId = widget.boardId。
- 调用点 2：_generatePlugin（定向导出）成功后 → createdBy 'scraper-capture'，
  boardId = widget.boardId。

### D2 · 新建 board/data_source_binding.dart（纯 Dart）

文件 lib/renderer/templates/scraper_modle/board/data_source_binding.dart

    class DataSourceInfo {
      final String name;          // data-xxx 的 xxx（manifest dataTypes[0].name 兜底目录名）
      final String pluginDir;     // 绝对路径
      final String displayName;   // dataTypes[0].displayName 兜底 name
      final String category;      // dataTypes[0].category
      final String? boardId;      // boundBoardId ?? boardId（创建画板）
      final String? createdBy;    // scraper-explore / scraper-capture / null（非爬虫创建）
      final Map<String, dynamic> rawManifest;
      bool get scraperMade => (createdBy ?? '').startsWith('scraper');
      String truncatedJson({int maxChars = 2000});  // 真实 JSON 截断（弹窗用）
      Map<String, dynamic> toSummaryJson();          // 名称/类型/字段/脚本存在性（bound_sources 用）
    }
    List<DataSourceInfo> scanDataSourcePlugins({String? pluginsRoot});
    // 扫描 <resolvePluginsRoot()>/*/data/manifest.json，type == 'data-source' 才收

### D3 + D4 · BoardContainerView 树状左栏 + 数据源视图

文件 lib/renderer/templates/scraper_modle/board/board_container_view.dart

用户需求逐条对齐：
1. 每个已注册数据源都要有画板；向画板 AI 告知数据状态（bound_sources.json /
   GeneratorView 动态扫描兜底，注入 _resumeContext）。
2. 同一画板（探索模式）创建的多个数据源 → 索引到同一画板（manifest.boardId）。
3. 左栏不再是平铺画板 → 画板-数据源树状结构：画板节点下缩进显示绑定源
   （boardId == 画板 id）；无画板的数据源归入「未绑定数据源」分组。
4. 支持切换数据源视图（头部 SegmentedButton：画板 / 数据源）。
5. 数据源视图：非爬虫创建的数据源显示空画板状态；点击后：
   a. 创建画板（capture 模式，名 = displayName）；
   b. 拷贝数据源脚本/JSON 到 boards/<id>/source/（data/ 目录内容 + config/config.json）；
   c. 写 boards/<id>/workflow.json（workflow.phase='debugging'、
      workflow.pythonCode=scraper.py 内容、dataName、pluginDir）；
   d. 写 boards/<id>/resume_prompt.txt（AI prompt：直接以现有脚本为起点调试，
      禁止重头演示抓包；读 source/manifest.json 与 source/scraper.py →
      一致性检查 → run_terminal_command 调试 → export_and_register_scraper）；
   e. 写 boards/<id>/bound_sources.json；
   f. 回写 manifest boundBoardId（绑定≠创建，创建者字段不动）；
   g. 选中新画板。
6. 数据源面板点击数据源：
   a. 先弹截断后的数据源真实 JSON（truncatedJson 弹窗，SelectableText）；
   b. 无对应画板 → 弹「建立画板？」确认框；
   c. 已有画板（boardId 命中 _boards）→ 直接切到该画板。

配套：view/scraper_generator_view.dart 的 _restoreBoardState() 兜底——
bound_sources.json 缺失时用 scanDataSourcePlugins() 按 boardId 过滤生成
_boundSourcesJson（探索创建的老画板也能向 AI 告知数据状态）。

### E/F · 收尾

- E：计划文档第七节已同步进度。
- F：用户本地验证通过后 commit（不 push；v2.0 受保护走 PR）。

---

## 三、关键数据格式约定

    <workspace>/scraper_output/            # greenixWorkspaceDir('scraper/scraper_output')
    ├── boards.json                        # BoardStore 画板元数据（已有）
    ├── boards/<boardId>/
    │   ├── workflow.json                  # {'workflow':..., 'dataName'?, 'pluginDir'?}
    │   ├── explore_workflow.json          # ExploreWorkflow.toJson()
    │   ├── session.json                   # [{'name','messages':[{'role','text'}],'createdAt'}]
    │   ├── agent_session_<idx>.json       # Agent Session.toJson() 完整上下文（侧车）
    │   ├── resume_prompt.txt              # 恢复后一次性发给 AI（数据源建板写入）
    │   ├── bound_sources.json             # 绑定数据源摘要数组（AI 数据状态告知）
    │   └── source/                        # 数据源建板时拷贝的脚本/JSON
    └── trace.jsonl                        # 已有

数据源插件 manifest 溯源字段（附加，不影响注册解析）：
boardId（创建画板）、boundBoardId（用户建板绑定，优先于 boardId）、
createdBy（scraper-explore / scraper-capture）。

---

## 四、验证状态（如实）

- Phase 2 所有改动未经任何编译验证（用户明确：验证与 debug 用户自己来）。
- Phase 1 仅跑过容器 dart analyze（0 error/warning）。Phase 2 编辑量大且用 python
  精确块替换，存在个别拼写/引用错误可能（导入缺失、getter 名字不一致），
  用户本地首次 flutter analyze + flutter test 以编译器为准逐条修。
- 需自查点：
  1) scraper_ai_panel.dart 的 _resumeExplore 引用 reopenSourcePicker（已存在）；
  2) GeneratorView 引用 ScraperAIPanel 新增构造参数与 dataName/pluginDir getter（A4 已加）；
  3) agent.Message/agent.Role/agent.Session 来自 core/agent/agent.dart barrel（已确认导出）。

---

## 五、环境备注

- Flutter SDK /home/node/flutter（3.47.0）；每条 dart/flutter 命令都要重新 export
  PATH / FLUTTER_STORAGE_BASE_URL / PUB_HOSTED_URL（镜像）。
- 容器 1GiB 内存：flutter analyze 会 OOM；dart analyze 单跑可用。
- .dart_tool 符号链接 → /tmp/evg-base-dart_tool（/tmp 重启清空，需重建 + flutter pub get；
  pub get 可能改写 analysis_options.yaml，记得 git checkout 还原）。
- 推送需用户新 PAT（旧 PAT 已泄露，务必吊销）。