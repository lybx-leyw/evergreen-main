# 变更内容简述

> 每次 `git pull` 后更新，只记录**本次**变更。过去的变更不记。

---

## 2026-07-06 23:32 — pull from origin/main

**提交范围**：`db444fe` → `0a6fae0`（Fast-forward）

**最近 5 个提交**：
1. `0a6fae0` — fix: SettingsView rewrite — direct Config API, no HTTP, no settings.exe
2. `053c960` — chore: test微调 + 任务清单文档
3. `7cd426c` — feat: V2 architecture alignment — layout engine, theme unification, HTML embed, settings
4. `db444fe` — feat: SettingsView 保存按钮 + Python 端移除 HTML UI

### 变更统计

| 维度 | 数值 |
|------|------|
| 文件变更数 | 218 files |
| 新增行数 | +24,904 |
| 删除行数 | -6,297 |
| 新增文件 | ~80+ |
| 删除文件 | ~30+ |

### 主要变更内容

1. **新增大量 CLAUDE.md 文档**
   - 根目录 `CLAUDE.md`（AI 协作规范）
   - `core/agent/CLAUDE.md`、`core/config/CLAUDE.md`、`core/data/CLAUDE.md`、`core/module/CLAUDE.md`、`core/theme/CLAUDE.md`
   - `renderer/CLAUDE.md`

2. **新增插件编写指南文档系列**
   - `plugin-authoring-guide-agent.md`
   - `plugin-authoring-guide-config.md`
   - `plugin-authoring-guide-core-services.md`
   - `plugin-authoring-guide-data.md`
   - `plugin-authoring-guide-module.md`
   - `plugin-authoring-guide-renderer.md`
   - `plugin-authoring-guide-theme.md`

3. **渲染层大幅增强**
   - 新增 `html/` HTML5 渲染引擎（html_components 1157行、html_template 660行、html_layout、html_style、html_renderer）
   - 新增 `render_tokens.dart`（625行 Design Tokens）
   - 新增 `slot_widgets.dart`（1387行，53种组件映射）
   - 新增 `calendar_widget.dart`、`file_tree.dart`、`lottery_wheel.dart`
   - 新增 `plugin_renderer.dart`、`html_render_view.dart`、`ai_assistant_slot.dart`
   - `chat_controller_view.dart` 大幅增强（+900 行）
   - `composite_view.dart` 重写（+800 行）
   - `settings_view.dart` 重写（从 487 行简化为 direct Config API）
   - 新增渲染层测试（layout_engine_test、module_dispatch_test、widgets_test）

4. **插件重组**
   - **删除** 15 个旧插件：base64、calculator、color-convert、json-format、password-gen、pomodoro、qr-text、text-utils、unit-convert、url-encode、uuid-gen、word-count、light
   - **新增** showcase（Python版，6个能力维度）
   - **新增** showcase-dart（Dart版，9个页面）
   - **新增** vocab-tutor 模块
   - core-settings 重命名为 settings

5. **核心层变化**
   - 新增 `page_event_bus.dart`（页面事件总线）
   - 新增 `expose_state_writer.dart`（状态暴露写入器）
   - `module_descriptor.dart` 重写（+2000 行改动）
   - ModuleRegistry、ModuleLoader、ModuleHttpServer 均有调整
   - dio_stub 新增（用于独立 dart analyze）
   - PLAN_NOW.md 删除

6. **其他**
   - 新增 `.gitignore` 7 条规则
   - 删除 `.module_port`（端口发现统一管理）
   - 新增 `test_manifest.dart`
   - 新增 `现在在做的事情.md`（任务清单）
   - pubspec.yaml 新增依赖

### 本次运行遇到的问题

- **MSVC 编译失败**：`webview_windows` 插件使用了已废弃的 `<experimental/coroutine>`，MSVC 14.51 将其视为 error。修复方式：`windows/CMakeLists.txt` 添加 `add_compile_definitions(_SILENCE_EXPERIMENTAL_COROUTINE_DEPRECATION_WARNINGS)`
- **DEEPSEEK_API_KEY 未设置**：AI 对话功能不可用
- **Python 解释器路径不存在**：`.greenix/python/python.exe` 未找到
