# 变更内容简述

> 工作区变更 vs 云端 HEAD (`0a80da0`)。覆盖已修改、已删除、新增未跟踪的全部变更。

---

## 变更统计

| 维度 | 数值 |
|------|------|
| 已修改文件 | 18 |
| 已删除文件 | 10 |
| 新增未跟踪文件 | ~100+ |
| 新增行数 | ~1,400 |
| 删除行数 | ~1,760 |

---

## 一、文档体系重构

### `.internal/` 内部文档重写

| 文件 | 变更 |
|------|------|
| `.internal/CLAUDE.md` | **新增**。六模块架构心智模型（项目定位→上中下游→进程管线→设计决策→数据结构→状态），替代原 `CLAUDE.md` 中的详细内容 |
| `.internal/RULES.md` | **新增**。工作规范 + 常见圈套 + Bug 处理（从全局 CLAUDE.md 迁移） |
| `.internal/EXPERIENCE.md` | **新增**。历史踩坑记录索引，2 条经验卡片 |
| `.internal/experiences/` | **新增**。2 条归档经验：`2026-07-05-python-plugin-no-html-ui.md`、`2026-07-06-reeditor-page-up-down-not-working.md` |
| `.internal/01-接轨文件.md` | **重写**。更新定位描述（多语言、AI 原生）、修正数据流向、composite→现状、安全网 3→4、删除可写范围/已知问题、补充快速通道 |
| `.internal/02-上游说明.md` | **重写**。修正 StormBreaker 阈值、补充 Compactor/AgentFactory/MemoryAgent/SkillGenerator、Agent 链路补充事后处理、新增子组件速查表 |
| `.internal/03-中游说明.md` | **重写**。manifest 示例 V1→V2、补充 dataSource 数据绑定、补充 .exe 标准契约、53→35+ 组件、更新插件列表 |
| `.internal/04-下游说明.md` | **重写**。新增快速通道章节（agent/data/config 含源码引用）、补充 PageEventBus、补充 ProcessManager 集成、更新组件分类 |
| `.internal/05-变更内容简述.md` | **重写**（即本文件）。改为工作区 vs HEAD 完整变更总结 |

### 根目录文档

| 文件 | 变更 |
|------|------|
| `CLAUDE.md` | **精简**。从 199 行完整架构文档缩减为 19 行索引，指向 `.internal/RULES.md` + `.internal/EXPERIENCE.md` + `.internal/experiences/` + `.refer_ui/` + `.reference/` + `PLAN.md` |
| `README.md` | **删除** |
| `AI_CHANGES_LOG.md` | **删除**（迁移至 `change_log/2/AI_CHANGES_LOG.md`） |
| `现在在做的事情.md` | **删除** |
| `补丁日志.md` | **删除** |
| `PLAN.md` | **新增**。当前实施计划 |
| `我做了的事情.md` | **新增**。个人任务记录 |
| `架构示意图.html` | **新增**。架构可视化 |

### 参考文档

| 目录/文件 | 变更 |
|-----------|------|
| `.refer_ui/` | **新增**。v1.4.0 渲染风格参考（`v1.4.0-modules.md` + 完整 `widget/lib/` 源码，~90 文件） |
| `change_log/` | **新增**。归档的变更日志 |

---

## 二、核心层变更（`evg-base/lib/core/`）

| 文件 | 行数变化 | 内容 |
|------|---------|------|
| `data/data_http_server.dart` | +21 | 数据 HTTP 服务增强 |

---

## 三、渲染层变更（`evg-base/lib/renderer/`）

| 文件 | 行数变化 | 内容 |
|------|---------|------|
| `shared/composite_view.dart` | +694 | **主力变更**。SlotDispatch 重构：chrome slot 工具栏分离、`_ButtonBar`/`_NavButton` 组件、`slot:switch_page` 页面导航事件、5 种布局范式完善（grid/flex/fullscreen/absolute/dock）、`_CalendarSlot`/`_LotteryWheelSlot` 等新 slot 实现 |
| `shared/html_render_view.dart` | +13 | WebView 初始化优化 |
| `shared/renderer_providers.dart` | +6 | 新增渲染层 Provider |
| `shared/data_dashboard_view.dart` | **新增** | 数据中枢面板，直接 `import core/data/data.dart`，调用 `DataOrchestrator` |
| `widgets/timetable_grid.dart` | **新增** | 课程表网格组件 |

---

## 四、宿主入口 + 基础设施（`evg-base/lib/`）

| 文件 | 行数变化 | 内容 |
|------|---------|------|
| `main.dart` | +97 | **新增** `_scanAndRegisterDataSources()`：启动时扫描 `plugins/<name>/data/manifest.json`，注册 `DataType` 到 `DataOrchestrator`。**时序修正**：data 注册在模块加载前完成，避免组件渲染时数据源未就绪 |
| `providers.dart` | +10 | 新增 Provider 声明 |
| `test/sprint2_test.dart` | +107 | **新增** Chrome slot 工具栏分离测试组（8 个 testWidgets）：chrome slot 无 Card 壳、工具栏 slot 独立渲染、slot:switch_page 事件导航、返回主页按钮等 |

---

## 五、插件层变更（`plugins/`）

### showcase（Python 版）— 归档

| 文件 | 变更 |
|------|------|
| `showcase/module/showcase_chat.py` | **删除** |
| `showcase/module/showcase_chat.exe` | **删除** |
| `showcase/module/showcase_export.py` | **删除** |
| `showcase/module/showcase_export.exe` | **删除** |
| `showcase/module/showcase_module.py` | **删除** |
| `showcase/module/showcase_module.exe` | **删除** |
| `showcase/module/showcase_page_code.py` | **删除** |
| `showcase/module/showcase_page_code.exe` | **删除** |
| `showcase/module/stdout.txt` | **删除** |

> Python 版 showcase 源码和 .exe 已移除，功能由 showcase-dart（Dart 渲染版）替代。

### showcase-dart（Dart 渲染版）

| 文件 | 行数变化 | 内容 |
|------|---------|------|
| `manifest.json` | +30 | **新增** `home` 主页（导航卡片网格，3 列 9 卡片，nav-button 组件）**新增** 8 个页面的 toolbar slot（每个页面顶部"返回主页"按钮，`slot:switch_page:home` 事件） |

### 新增插件

| 插件 | 维度 | 内容 |
|------|------|------|
| `plugins/courses/` | module + config + data | 课程模块（`manifest.json` + `config.json` + `zdbk_data.exe`/`.py`） |
| `plugins/data-dashboard/` | module | 数据中枢面板模块（`manifest.json`） |
| `plugins/showcase-dart-chrome/` | module | 展示大厅 Chrome 变体（6 个 .exe + 6 个 .py + manifest.json） |

---

## 六、端口文件 + 配置

| 文件 | 变更 |
|------|------|
| `.agent_port` `.config_port` `.core_port` `.data_port` `.module_port` `.theme_port` | 端口号更新（运行时自动写入） |
| `.gitignore` | -1 行（规则调整） |

---

## 七、变更摘要

| 分类 | 要点 |
|------|------|
| **文档** | `.internal/` 全部重写与 `.internal/CLAUDE.md` 对齐；根 `CLAUDE.md` 精简为索引；删除 README/旧日志 |
| **渲染** | `composite_view.dart` +694 行：chrome slot 分离、页面导航事件、新 slot 组件 |
| **入口** | `main.dart` +97 行：data 插件预扫描注册，修正时序 |
| **测试** | `sprint2_test.dart` +107 行：Chrome slot 测试组 |
| **插件** | Python showcase 归档删除；showcase-dart 新增主页 + toolbar；新增 courses/data-dashboard/showcase-dart-chrome 三个插件 |
| **参考** | `.refer_ui/` ~90 文件：v1.4.0 渲染风格完整参考 |
