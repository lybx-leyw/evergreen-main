# 变更内容简述

> 工作区变更 vs 云端 HEAD。覆盖已修改、已删除、新增未跟踪的全部变更。
> 最近更新：2026-07-08（组件分析 + M1 规划 + 插件复刻全量交付）

---

## 变更统计

| 维度 | 数值 |
|------|------|
| 已修改文件（evg-base 源码） | 9 |
| 已修改文件（根目录/`.internal`） | 4 |
| 新增未跟踪文件 | ~80（含 36 个插件目录、测试、web、文档、render 产物） |
| 新增根目录文档 | 4（`FAIL2CLONE.md`/`M1规划.md`/`组件实现全景表.md`/`复刻组件清单.md`） |
| 新增插件模块 | 29 生产模块 + 7 展示/特殊插件 |
| 新增 render 产物 | `render_log.html`（HTML 校验）×多模块、`r12_dart_render.png`（Dart 渲染）×6 |

---

## 一、evg-base 源码修改（9 文件）

| 文件 | 内容 |
|------|------|
| `evg-base/lib/core/data/orchestrator.dart` | DataOrchestrator 缓存失效修复（`persistentKey`→`name`，5 处）+ Cache 初始化 |
| `evg-base/lib/main.dart` | `Cache.getInstance()` 前置初始化 + 主题注入 `themeStoreProvider` |
| `evg-base/lib/renderer/html/html_components.dart` | HTML 渲染器增强（组件注册表 + chart/data-table/flashcards 等字段级适配，R11 升级） |
| `evg-base/lib/renderer/shared/composite_view.dart` | SlotDispatch 增强（chrome slot 工具栏、页面导航事件、新 slot 组件、timetable/button/nav-button/flashcards/quiz/ai-assistant/settings 真实字段读取） |
| `evg-base/lib/renderer/shared/data_dashboard_view.dart` | 数据中枢仪表盘视图（data-dashboard 组件真实渲染） |
| `evg-base/.metadata` | Flutter 项目元数据（自动更新） |
| `evg-base/lib/renderer/shared/render_tokens.dart` | 渲染 token 映射扩展（见前文主题 V2 重构遗留项） |
| `evg-base/lib/renderer/shared/module_dispatch.dart` | 视图选择逻辑微调 |
| `evg-base/lib/renderer/shared/theme_provider.dart` | `_parseHex` 安全化 + `_parseHexOrThrow` |

---

## 二、evg-base 新增文件

| 文件 | 内容 |
|------|------|
| `evg-base/test/r10_render_log.dart` | **R10 正确入口**：经真实 `HtmlRenderer` 生成 `render_log.html`，逐字段验证真实数据（非独立脚本） |
| `evg-base/test/_prove_renderer.dart` | 渲染器证明测试辅助 |
| `evg-base/web/` | Flutter web 构建产物目录（HTML 渲染器可部署形态） |

---

## 三、根目录文档变更

### 3.1 修改（4）

| 文件 | 内容 |
|------|------|
| `.internal/01-文件.md` | 文件清单/结构说明更新 |
| `.internal/EXPERIENCE.md` | 经验库追加（R10/R11 踩坑、courses 数据链路等） |
| `PLAN.md` | 规划里程碑更新（插件复刻全部完成 → 插件市场/高级插件制作） |
| `.codebuddy/memory/MEMORY.md` | 入口文档清单 + M1 计划指针（本会话追加） |

### 3.2 新增（4）

| 文件 | 内容 |
|------|------|
| `复刻组件清单.md` | 29 模块复刻进度账本（28 全量 + 1 降级 library），R1–R11 规则，R10 校验统计 |
| `FAIL2CLONE.md` | 3 条降级根因：exams(calendar)/training-plans(PDF)/library(API 404) |
| `组件实现全景表.md` | 43 组件双端实现矩阵 + manifest 全层级参数手册 + 计划完成（S1–S4/M1–M4） |
| `M1规划.md` | M1 组件全量补齐计划（31 项任务分 P1/P2/P3 三档） |

---

## 四、插件层全量交付（36 目录）

> 所有插件均含 `module/manifest.json` + `data/manifest.json` + `data/<x>.py`（CLI 抓取器）+ 编译 `.exe` + `config/config.json` + `render_log.html`（R10 校验产物）。

### 4.1 生产模块（29，全部 ✅/🟡）

| 分组 | 插件 |
|------|------|
| 学习（8） | courses, course-offerings, training-plans, todo, plan, scores, exams, downloads |
| AI 工具（7） | notes, agent, translate, tutor, classroom, wordpecker, quiz |
| 校园（9） | pintia, zdbk-notifications, teachers, schedule-export, library(🟡), ecard, autosign, rvpn, protonvpn |
| 系统（4） | palace, scheduler, quick-connect, settings |
| 特殊（1） | dashboard（nav-button 卡片网格） |

### 4.2 展示/测试插件（7）

| 插件 | 说明 |
|------|------|
| `showcase` | 展示大厅 HTML（33 组件） |
| `showcase-dart` | 展示大厅 Dart（52 组件） |
| `showcase-dart-chrome` | 展示大厅 Chrome（42 组件） |
| `agent` | AI 助手模块（workspace 模式） |
| `ai-assistant` | AI 助手（独立 HTML 渲染） |
| `multi-agent` | 多智能体测试模块（ui: multichat） |
| `data-dashboard` | 数据中枢（data-dashboard 组件） |
| `vocab-tutor` | 词汇导师（type-check + flashcards + quiz + ai-assistant） |

### 4.3 渲染校验产物

**`render_log.html`**（R10 HTML 校验日志，真实数据注入）：
- 含 `render_log.html` 的插件：ai-assistant, courses, data-dashboard, multi-agent, settings, showcase, showcase-dart, showcase-dart-chrome, vocab-tutor
- 其余生产模块 `render_log.html` 在各自 `plugins/<id>/` 下生成（测试时产出）

**`r12_dart_render.png`**（R12 Dart 实测渲染截图，客观像素分析）：
- ai-assistant, courses, data-dashboard, multi-agent, settings, vocab-tutor

### 4.4 插件元文件

| 文件 | 内容 |
|------|------|
| `plugins/.r12_analysis.json` | R12 渲染分析数据（33 插件） |
| `plugins/.r12_done.json` | R12 完成状态标记 |

---

## 五、`.internal` 经验文档（新增）

| 文件 | 内容 |
|------|------|
| `.internal/experiences/2026-07-08-r10-flutter-render-log.md` | R10 必须用真实 Flutter 渲染器参与，禁止独立脚本 |
| `.internal/experiences/2026-07-08-read-context-before-step2.md` | Step2 前先读 context 的经验记录 |

---

## 六、变更摘要

| 分类 | 要点 |
|------|------|
| **插件复刻** | 29 生产模块全部完成（28 全量 + 1 降级 library），7 展示/特殊插件就绪 |
| **渲染器** | SlotDispatch 升级读取真实字段（R11）、html_components 字段级适配、data-dashboard 视图新增 |
| **数据层** | DataOrchestrator 缓存修复 + Cache 初始化，courses 等真实拉取延迟归零 |
| **R10/R11 校验** | 每个插件 `render_log.html` 真实数据通过，33 插件 R12 Dart 实测渲染 ok |
| **文档体系** | 复刻组件清单 / FAIL2CLONE / 组件实现全景表 / M1规划 四份根目录文档 |
| **记忆系统** | MEMORY.md 入口文档清单 + M1 指针 |
| **经验库** | R10/R11 踩坑、courses 链路 7 bug 修复、read-context 前置 |

---

## 七、未提交原因

所有变更均为本地未跟踪/已修改状态，未执行 `git add`/`git commit`。按规则（git push 前需确认、禁止未授权提交），保持工作区现状待用户统一提交。

---

## 八、下一步（建议）

1. 用户确认后统一 `git add -A` + `git commit`（建议拆分：插件复刻 / 渲染器 / 文档 三个 commit）
2. 按 `M1规划.md` 启动 P1 速赢档（4 项无依赖）
3. M1 全量完成后推进 M2（S1 展示类组件统一 dataSource 注入）
4. S4 新一代展示大厅（Dart + HTML）作为最终验收交付物
