# 组件实现全景表

> 生成日期：2026-07-08 | 源文件：`composite_view.dart` SlotDispatch + `html_components.dart` renderers
> 覆盖范围：43 个具名组件 + 20 个 placeholder 预留位 + manifest 全层级可选参数

---

## 一、组件类型实现表

状态图例：✅已实现 ⚠️部分实现 ❌未实现/占位

### 智能交互（7）

| # | type | 功能简介 | 关键 config 字段 | Dart 视图 | HTML 渲染 | 备注 |
|---|------|---------|-----------------|----------|----------|------|
| 1 | `ai-assistant` | AI 助手对话（嵌入模式） | preset, system_prompt, tools, multi_session, global_memory, bubble, thinking, stream | ✅ ChatControllerView | ✅ `_renderChat`（别名） | 与 chat 同渲染，语义别名 |
| 2 | `chat` | 通用聊天窗口（嵌入模式） | 同上 | ✅ ChatControllerView | ✅ `_renderChat` | 参数字段完整适配 |
| 3 | `form` | 结构化表单 | fields[].type/key/label/required/options/placeholder, submitLabel | ✅ FormView | ✅ `_renderForm` | 字段级动态渲染 |
| 4 | `settings` | 设置面板 | settings[].label/key/type/hint/value/default/options | ✅ SettingsView | ✅ `_renderSettings` | 读 config.json 真实设置项 |
| 5 | `data-dashboard` | 数据中枢仪表盘 | 复用 dataBindings | ✅ DataDashboardView | ❌ 未注册 | slot 专用，HTML 无独立实现 |
| 6 | `code-editor` | 代码编辑器 | language, content, readonly, lineNumbers | ✅ EditorView | ⚠️ `_renderCodeEditor` | HTML: language 读 config，代码内容硬编码示例 |
| 7 | `prompt-builder` | 提示词构建器 | template, variables{} | ❌ \_UnknownSlot | ⚠️ `_renderPromptBuilder` | HTML: template/variables 读 config，空时硬编码默认值 |

### 数据展示（8）

| # | type | 功能简介 | 关键 config 字段 | Dart 视图 | HTML 渲染 | 备注 |
|---|------|---------|-----------------|----------|----------|------|
| 8 | `data-table` | 数据表格 | columns[], rows[], title, display, filter, sortable | ✅ \_DataTableSlot | ✅ `_renderDataTable` | 最成熟组件，支持异步数据源拉取 |
| 9 | `card-list` | 卡片列表 | title, cards[].image/title/body/footer, columns | ✅ DefaultView | ✅ `_renderCardList` | card-list 为空时退化为 data-card 网格 |
| 10 | `chart` | 数据图表 | chart.type/title/legend | ✅ DashboardView | ⚠️ `_renderChart` | HTML: type/title/legend 读 config，数据系列硬编码 |
| 11 | `stat-tile` | 统计指标卡片 | title, value, subtitle, trend, trendUp, icon | ✅ DashboardView | ✅ `_renderStatTile` | 字段完整适配 |
| 12 | `kanban` | 看板视图 | columns[].title/items[] | ✅ DefaultView | ⚠️ `_renderKanban` | HTML: columns 读 config，空时硬编码 3 列 |
| 13 | `tree` | 树形结构 | root.label/children[]/icon | ❌ \_UnknownSlot | ⚠️ `_renderTree` | HTML: root 读 config，null 时硬编码 3 级树 |
| 14 | `timeline` | 时间线 | title, items[].time/label/description | ✅ DashboardView | ✅ `_renderTimeline` | 字段完整适配 |
| 15 | `map` | 地图 | map.center.lat/lng, zoom, markers[] | ✅ \_MapSlot | ⚠️ `_renderMap` | HTML: center 读 config，地图为静态占位符（无真实地图） |

### 文档与媒体（12）

| # | type | 功能简介 | 关键 config 字段 | Dart 视图 | HTML 渲染 | 备注 |
|---|------|---------|-----------------|----------|----------|------|
| 16 | `doc-viewer` | 文档查看器 | document.exportFormats, content | ✅ DocumentView | ⚠️ `_renderDocument`（别名） | HTML: exportFormats 读 config，文档内容硬编码 |
| 17 | `doc-editor` | 文档编辑器 | 同 document | ✅ DocumentView | ⚠️ `_renderDocument`（别名） | 与 doc-viewer 同渲染 |
| 18 | `document` | 文档（通用） | 同上 | ✅ DocumentView | ⚠️ `_renderDocument` | 同上 |
| 19 | `video-player` | 视频播放器 | url, title | ✅ \_VideoSlot | ✅ `_renderVideo`（别名） | url 为空时显示占位播放器 |
| 20 | `video` | 视频（通用） | 同上 | ✅ \_VideoSlot | ✅ `_renderVideo` | 同上 |
| 21 | `audio-player` | 音频播放器 | src, title | ❌ \_UnknownSlot | ✅ `_renderAudioPlayer` | HTML 已实现，Dart 未实现 |
| 22 | `image-gallery` | 图片画廊 | title, images[].url/caption | ❌ \_UnknownSlot | ✅ `_renderImageGallery` | HTML 已实现，Dart 未实现 |
| 23 | `presentation` | 幻灯片 | 预留 | ✅ PresentationView | ❌ `_renderPresentation` | HTML 完全不读 config，全硬编码 |
| 24 | `nav-button` | 导航按钮卡片 | label, icon, target | ✅ \_NavButton | ✅ `_renderNavButton` | dashboard 核心组件，emit switch_page 事件 |
| 25 | `button` | 按钮组 | align, buttons[].label/icon/event/style | ✅ \_ButtonBar | ✅ `_renderButton` | 支持 5 种样式，row/column 方向 |
| 26 | `timetable` | 周课表 | sessions[].courseName/teacher/location/dayOfWeek/periods | ✅ \_TimetableSlot | ✅ `_renderTimetable` | 7 列网格，支持异步数据源 |
| 27 | `markdown` | Markdown 渲染 | content, src | ✅ \_MarkdownSlot | ✅ `_renderMarkdown` | content 直接文本，src 暂未实现文件加载 |

### 创作与工具（6）

| # | type | 功能简介 | 关键 config 字段 | Dart 视图 | HTML 渲染 | 备注 |
|---|------|---------|-----------------|----------|----------|------|
| 28 | `spreadsheet` | 电子表格 | 预留 | ✅ SpreadsheetView | ❌ `_renderSpreadsheet` | HTML 完全不读 config，5×5 硬编码 |
| 29 | `notepad` | 记事本 | content, placeholder | ❌ \_UnknownSlot | ✅ `_renderNotepad` | HTML 已实现，Dart 未实现 |
| 30 | `whiteboard` | 白板 | 预留 | ❌ \_UnknownSlot | ❌ `_renderWhiteboard` | 两端均未实现，HTML 读 cfg 但丢弃不用 |
| 31 | `mindmap` | 思维导图 | 预留 | ✅ MindmapSlot | ❌ `_renderMindmap` | HTML 完全不读 config，"核心主题"硬编码 |
| 32 | `diff-viewer` | 差异对比 | leftLabel, rightLabel | ❌ \_UnknownSlot | ⚠️ `_renderDiffViewer` | HTML: label 读 config，diff 行硬编码 |
| 33 | `terminal` | 终端模拟器 | cwd | ❌ \_UnknownSlot | ⚠️ `_renderTerminal` | HTML: cwd 读 config，输出行全硬编码 |

### 学习专用（5）

| # | type | 功能简介 | 关键 config 字段 | Dart 视图 | HTML 渲染 | 备注 |
|---|------|---------|-----------------|----------|----------|------|
| 34 | `type-check` | 打字练习 | wordList, question, options, score | ✅ TypeCheckSlot | ❌ `_renderTypeCheck` | HTML 完全不读 config，固定 Python 题目 |
| 35 | `flashcards` | 闪卡记忆 | wordList[].word/meaning, algorithm | ✅ FlashcardsSlot | ✅ `_renderFlashcards` | 字段完整适配 |
| 36 | `quiz` | 选择题测验 | wordList, questionTypes, timeLimit, passScore | ✅ QuizSlot | ✅ `_renderQuiz` | 从 wordList 生成真实题目 |
| 37 | `crossword` | 填字游戏 | title | ❌ \_UnknownSlot | ⚠️ `_renderCrossword` | HTML: title 读 config，网格硬编码 |
| 38 | `pronunciation` | 发音训练 | word, phonetic | ❌ \_UnknownSlot | ⚠️ `_renderPronunciation` | HTML: word/phonetic 读 config，分数硬编码 |

### 特殊（3）

| # | type | 功能简介 | 关键 config 字段 | Dart 视图 | HTML 渲染 | 备注 |
|---|------|---------|-----------------|----------|----------|------|
| 39 | `custom` | 自定义 HTML | html, src | ❌ \_UnknownSlot | ✅ `_renderCustom` | HTML 支持 raw HTML 注入或 iframe，Dart 未实现 |
| 40 | `webview` | 内嵌网页 | url, allowScripts | ❌ \_UnknownSlot | ✅ `_renderWebView` | HTML 已实现，Dart 未实现 |
| 41 | `divider` | 分割线 | 无 | ✅ \_DividerSlot | ✅ `_renderDivider` | 纯视觉分隔，两端均实现 |

### 扩展组件（2）

| # | type | 功能简介 | 关键 config 字段 | Dart 视图 | HTML 渲染 | 备注 |
|---|------|---------|-----------------|----------|----------|------|
| 42 | `lottery-wheel` | 抽奖转盘 | lottery.title/subtitle/buttonText, segments[] | ✅ \_LotteryWheelSlot | ⚠️ `_renderLotteryWheel` | HTML: title 读 config，段硬编码；Dart: 有 Canvas 自绘 |
| 43 | `calendar` | 月历 | events[].date/title | ✅ \_CalendarSlot | ❌ `_renderCalendar` | HTML 完全不读 config，2026-07 硬编码 |

### 预留扩展（20）

| # | type | 功能简介 | Dart 视图 | HTML 渲染 |
|---|------|---------|----------|----------|
| 44-63 | `placeholder-01` ~ `placeholder-20` | 预留扩展位 | ❌ \_UnknownSlot（「预留扩展」分组） | ❌ 无渲染函数（兜底通用占位卡片） |

---

## 二、组件可选参数全览

### 2.1 ComponentDescriptor（组件级）

| 参数 | 类型 | 层级 | 说明 |
|------|------|------|------|
| `type` | `string` | 必填 | 组件类型名，必须匹配上述 43 个之一 |
| `config` | `object` | 可选 | 透传给组件的配置（字段因 type 而异） |
| `input` | `InputOptions` | 可选 | 输入配置（聊天框/表单等交互式组件） |
| `events` | `EventDescriptor` | 可选 | 组件级事件（emit + listen + delegates） |
| `process` | `ProcessDescriptor[]` | 可选 | 组件级后端进程 |
| `dataSource` | `DataSourceDescriptor` | 可选 | 组件数据源（独立于页面级 dataSource） |
| `theme` | `object` | 可选 | 组件级主题覆盖 `{"components": {...}}` |

### 2.2 InputOptions（输入配置）

| 字段 | 类型 | 说明 |
|------|------|------|
| `mode` | `"text"`\|`"voice"`\|`"code"`\|`"markdown"` | 输入模式 |
| `autoFocus` | `boolean` | 是否自动聚焦 |
| `multiline` | `boolean` | 是否多行 |
| `sendOnEnter` | `boolean` | Enter 发送（默认 Ctrl+Enter） |
| `voice.language` | `string` | 语音识别语言（如 zh-CN） |
| `voice.continuous` | `boolean` | 持续识别 |
| `placeholder` | `string` | 占位文本 |
| `maxLength` | `integer` | 最大输入长度 |
| `quickReplies` | `string[]` | 快捷回复列表 |
| `fixedSize` | `FixedSizeOptions` | 固定尺寸（width/height） |

---

## 三、Slot 级可选参数

**SlotDescriptor** — layout.slots.{slotKey}

| 参数 | 类型 | 说明 |
|------|------|------|
| `style` | `StyleDescriptor` | 插槽样式（width/height/padding/margin/flex/grid/position 等） |
| `process` | `ProcessDescriptor[]` | 插槽级后端进程 |
| `events` | `EventDescriptor` | 插槽级事件（delegates 声明交互委托） |
| `component` | `ComponentDescriptor` | 组件声明（必填，slot 不能嵌套 slot） |
| `theme` | `object` | slot 级主题覆盖 `{"slot": {...}}` |

---

## 四、Layout 级可选参数

**LayoutDescriptor** — pages[].layout

| 参数 | 类型 | 说明 |
|------|------|------|
| `type` | `"grid"`\|`"flex"`\|`"fullscreen"`\|`"absolute"`\|`"dock"` | 布局范式（默认 grid） |
| `preset` | `LayoutPreset` | 布局预设超参数 |
| `preset.columns` | `integer` | grid: 列数 |
| `preset.rows` | `string` | grid: 行数 |
| `preset.direction` | `string` | flex: 方向 |
| `preset.wrap` | `boolean` | flex: 是否换行 |
| `preset.gap` | `float` | 通用: 槽间距 |
| `preset.justify` | `string` | flex: 主轴对齐 |
| `preset.align` | `string` | flex: 交叉轴对齐 |
| `preset.regions` | `object` | dock: 各区域配置 |
| `features` | `LayoutFeatures` | 布局特性 |
| `features.zoom.enabled` | `boolean` | 是否启用缩放 |
| `features.zoom.min` | `float` | 最小缩放 |
| `features.zoom.max` | `float` | 最大缩放 |
| `features.search.enabled` | `boolean` | 是否启用搜索栏 |
| `features.search.placeholder` | `string` | 搜索占位文本 |
| `features.drawers` | `string[]` | 侧边抽屉列表 |
| `style` | `StyleDescriptor` | 布局级样式 |
| `events` | `EventDescriptor` | 布局级事件 |
| `slots` | `Map<string, SlotDescriptor>` | 插槽映射 |

---

## 五、Page 级可选参数

**PageDescriptor** — module.pages[]

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | `string` | 页面唯一标识（模块内，必填） |
| `label` | `string` | 页面标签（必填，显示在 TabBar） |
| `route` | `string` | 页面独立路由（V2: 不再自动拼接模块路由） |
| `isDefault` | `boolean` | 是否为默认页面 |
| `singlePage` | `boolean` | 单页面模式（不显示子导航 TabBar） |
| `events` | `EventDescriptor` | 页面级事件 |
| `process` | `ProcessDescriptor[]` | 页面级后端进程 |
| `dataSource` | `DataSourceDescriptor` | 页面级数据源 |
| `layout` | `LayoutDescriptor` | 布局（必填，默认 grid） |
| `theme` | `object` | 页面级主题覆盖 `{"page": {...}}` |

---

## 六、Module 级可选参数

**ModuleDescriptor** — manifest.json 根节点

| 参数 | 类型 | 说明 |
|------|------|------|
| `schemaVersion` | `string` | Schema 版本（必须为 "2.0"） |
| `id` | `string` | 模块唯一标识（必填） |
| `name` | `string` | 模块显示名称（必填） |
| `description` | `string` | 模块描述 |
| `icon` | `integer` | Material Icons codePoint |
| `version` | `string` | 版本号，默认 "0.0.0" |
| `route` | `string` | 模块路由 |
| `dependencies` | `string[]` | 依赖模块 ID 列表 |
| `activateSkills` | `string[]` | 激活的 Skill 名列表 |
| `style` | `StyleDescriptor` | 模块级默认样式 |
| `nav` | `NavObjectDescriptor` | 导航配置 |
| `nav.sidebar` | `SidebarDescriptor` | 侧边栏入口 |
| `nav.secondary` | `NavDescriptor[]` | 二级导航 |
| `process` | `ProcessDescriptor[]` | 模块级后端进程 |
| `events` | `EventDescriptor` | 模块级事件 |
| `actions` | `ActionDescriptor` | 交互动作 |
| `dataBindings` | `DataBindingDescriptor[]` | 数据绑定列表（JSON 字段: "data"） |
| `workspace` | `WorkspaceDescriptor` | 文件工作区配置 |
| `pages` | `PageDescriptor[]` | 页面列表 |
| `theme` | `object` | 模块级主题覆盖 `{"module": {...}}` |

---

## 七、Action 动作参数

**ActionDescriptor** — module.actions

| 参数 | 类型 | 说明 |
|------|------|------|
| `itemTap` | `string` | 列表项点击行为 |
| `itemLongPress` | `string` | 列表项长按行为 |
| `itemSwipe` | `string` | 列表项滑动行为 |
| `selection` | `"none"`\|`"single"`\|`"multi"` | 选择模式 |
| `refresh.enabled` | `boolean` | 是否启用刷新 |
| `refresh.pullToRefresh` | `boolean` | 下拉刷新 |
| `refresh.autoInterval` | `integer` | 自动刷新间隔（秒） |
| `sortable` | `string[]` | 可排序字段名列表 |
| `creatable` | `boolean` | 是否可创建 |
| `editable` | `boolean` | 是否可编辑 |
| `deletable.enabled` | `boolean` | 是否可删除 |
| `deletable.confirm` | `boolean` | 删除前是否确认 |
| `exportable` | `string[]` | 可导出格式（pdf/csv/excel/json） |
| `actionButtons` | `ActionButtonDescriptor[]` | V2: 动作按钮列表 |
| `actionButtons[].id` | `string` | 按钮唯一标识 |
| `actionButtons[].trigger` | `string` | 触发器标识（如 "button:quick-translate"） |
| `actionButtons[].label` | `string` | 按钮标签 |
| `actionButtons[].icon` | `integer` | 按钮图标 codePoint |
| `actionButtons[].process` | `ProcessDescriptor` | 动作级后端进程 |

---

## 八、事件系统参数

**EventDescriptor** — 可用于 module/page/layout/slot/component 任意层级

| 参数 | 类型 | 说明 |
|------|------|------|
| `emit` | `string[]` \| `EventEmitDescriptor[]` | 产生的事件名列表（snake_case） |
| `listen` | `EventListenDescriptor[]` | 关注的事件 |
| `listen[].event` | `string` | 事件名 |
| `listen[].source` | `string` | 来源 slot（可选，空=接受所有来源） |
| `listen[].filter` | `object` | 过滤条件（可选） |
| `listen[].handler` | `string` | 处理函数名 |
| `delegates` | `EventDelegatesDescriptor` | 交互委托 |
| `delegates.onClick` | `DelegationAction` | 点击 → navigate/emit/toggle/submit |
| `delegates.onKeyPress` | `DelegationAction` | 按键 → submit/navigate/emit（需 key 字段） |
| `delegates.onHover` | `DelegationAction` | 悬停 → tooltip/highlight |
| `delegates.propagate` | `DelegationAction` | 传播事件到总线 |

**Delegate action 参数：**
| action | 必需参数 | 可选参数 |
|--------|---------|---------|
| `navigate` | `target` (pageId) | — |
| `emit` | `event` | `data` |
| `submit` | — | `target` (slotKey) |
| `toggle` | `target` (CSS 选择器) | — |
| `tooltip` | `text` | — |
| `highlight` | — | `color`, `duration` |

**系统保留事件名：** `test_started`, `test_completed`, `question_answered`, `card_flipped`, `card_known`, `card_forgotten`, `answer_correct`, `answer_wrong`, `word_completed`, `reset_round`

---

## 九、后端进程参数

**ProcessDescriptor** — module/page/layout/slot/component/actionButton 均可声明

| 参数 | 类型 | 说明 |
|------|------|------|
| `id` | `string` | 进程唯一标识 |
| `exe` | `string` | 可执行文件路径 |
| `protocol` | `string` | 通信协议（stdin/stdout/http/websocket） |
| `scope` | `string` | 作用域 |
| `autoStart` | `boolean` | 模块加载时自动启动 |
| `autoRestart` | `boolean` | 崩溃后自动重启 |
| `preferredPort` | `integer` | 期望端口号 |

---

## 十、数据源参数

**DataSourceDescriptor** — page/component 层级可用

| 参数 | 类型 | 说明 |
|------|------|------|
| `endpoint` | `string` | 数据端点 URL |
| `method` | `string` | HTTP 方法（GET/POST） |
| `dataPath` | `string` | 响应 JSON 中数据所在路径 |
| `transform` | `string` | 数据转换方法名 |
| `refreshInterval` | `integer` | 自动刷新间隔（秒） |

---

## 十一、统计汇总

| 维度 | 数值 |
|------|------|
| 具名组件类型 | **43** |
| placeholder 预留位 | **20**（placeholder-01~20） |
| Dart 端已实现组件 | **28** / 43（65%） |
| Dart 端未实现（占位） | **15** / 43（35%） |
| HTML 端完整适配 | **21** / 38（55%） |
| HTML 端部分适配 | **10** / 38（26%） |
| HTML 端完全硬编码 | **7** / 38（18%） |
| 两端均实现 | **22** |
| 仅 Dart 实现 | **2**（data-dashboard, audio-player*） |
| 仅 HTML 实现 | **6**（audio-player, image-gallery, notepad, custom, webview, pronunciation） |

> *audio-player: Dart SlotDispatch 为 \_UnknownSlot，但有 widgets/audio_player.dart 原子组件

---

## 十二、组件类型 → config 关键字段速查

| 组件 type | config 结构示例 |
|----------|----------------|
| `chat` / `ai-assistant` | `{"preset": "tutor", "system_prompt": "...", "tools": ["search"], "multi_session": true, "global_memory": true, "thinking": {"visible": true}, "bubble": {"showAvatar": true}}` |
| `form` | `{"form": {"fields": [{"type": "text", "key": "name", "label": "姓名", "required": true}], "submitLabel": "提交"}}` |
| `settings` | `{"settings": [{"label": "用户名", "key": "username", "type": "string", "value": ""}]}` |
| `code-editor` | `{"language": "python", "content": "...", "readonly": false, "lineNumbers": true}` |
| `data-table` | `{"title": "...", "columns": [{"key": "name", "label": "名称"}], "rows": [...], "filter": true, "sortable": true}` |
| `chart` | `{"chart": {"type": "bar", "title": "...", "legend": true}}` |
| `stat-tile` | `{"title": "...", "value": "42", "subtitle": "...", "trend": "+12%", "trendUp": true, "icon": "..."}` |
| `card-list` | `{"title": "...", "cards": [{"image": "...", "title": "...", "body": "...", "footer": "..."}]}` |
| `kanban` | `{"columns": [{"title": "待办", "items": [{"title": "..."}]}]}` |
| `tree` | `{"root": {"label": "根", "children": [{"label": "子"}]}}` |
| `timeline` | `{"title": "...", "items": [{"time": "..." , "label": "...", "description": "..."}]}` |
| `map` | `{"map": {"center": {"lat": 30.27, "lng": 120.12}, "zoom": 12, "markers": []}}` |
| `document` | `{"document": {"exportFormats": ["pdf", "docx"]}}` |
| `video` / `video-player` | `{"url": "https://..."}` |
| `audio-player` | `{"src": "https://...", "title": "..."}` |
| `image-gallery` | `{"title": "...", "images": [{"url": "...", "caption": "..."}]}` |
| `presentation` | `{"presentation": {}}` |
| `nav-button` | `{"label": "课程", "icon": "📚", "target": "courses"}` |
| `button` | `{"align": "center", "buttons": [{"label": "导出", "icon": "📤", "style": "filled", "event": "export:pdf"}]}` |
| `timetable` | `{"sessions": [{"courseName": "...", "teacher": "...", "location": "...", "dayOfWeek": 1, "periods": [1,2]}]}` |
| `markdown` | `{"content": "# 标题\n\n正文..."}` |
| `notepad` | `{"content": "...", "placeholder": "输入内容..."}` |
| `flashcards` | `{"wordList": [{"word": "hello", "meaning": "你好"}], "algorithm": "sm2"}` |
| `quiz` | `{"wordList": [...], "questionTypes": ["choice"], "timeLimit": 60, "passScore": 80}` |
| `type-check` | `{"wordList": [...], "question": "...", "options": [...], "score": 0}` |
| `pronunciation` | `{"word": "hello", "phonetic": "/həˈloʊ/"}` |
| `crossword` | `{"title": "..."}` |
| `lottery-wheel` | `{"lottery": {"title": "抽奖", "subtitle": "...", "buttonText": "开始"}}` |
| `calendar` | `{"events": [{"date": "2026-07-15", "title": "..."}]}` |
| `prompt-builder` | `{"template": "...", "variables": {"role": "Python 专家"}}` |
| `diff-viewer` | `{"leftLabel": "原文件", "rightLabel": "新文件"}` |
| `terminal` | `{"cwd": "~/projects"}` |
| `webview` | `{"url": "https://...", "allowScripts": true}` |
| `custom` | `{"html": "<div>...</div>"}` 或 `{"src": "https://..."}` |
| `data-dashboard` | 不需要额外 config，复用 module.dataBindings |
| `divider` | 不需要 config |
| `spreadsheet` | 预留（暂无 config 规范） |
| `whiteboard` | 预留（暂无 config 规范） |
| `mindmap` | 预留（暂无 config 规范） |

---

## 十三、主题覆盖层级

从外到内，共 6 层主题覆盖（括号内为 JSON 键值）：

```
module.theme ("module") 
  └─ page.theme ("page")
       └─ layout.theme ("layout")
            └─ slot.theme ("slot") 
                 └─ component.theme ("components")
                      └─ RenderTokens 默认色板
```

内层覆盖外层同名字段，未覆盖字段从外层继承。

---

## 十四、计划完成

> 配套：`FAIL2CLONE.md`（3 条降级记录）  
> 已有组件的实现状态（✅/⚠️/❌）见第一章，本章只列跨组件的结构性改造和全新需求。

### 14.1 FAIL2CLONE 处置策略

| # | 来源 | 问题 | 策略 |
|---|------|------|------|
| 1 | `exams` | `calendar` 不支持 `dataSource` 动态注入 | → **S1: 展示类组件统一 dataSource 注入**（见 14.2） |
| 2 | `training-plans` | 无 PDF 渲染组件 | → **S2: 新增 `pdf-viewer` 组件类型**（见 14.2） |
| 3 | `library` | 借阅数据接口现网 404 | 不动。上游 API 废弃，代码已完整实现，接口恢复即用 |

### 14.2 新增待办（跨组件改造 + 全新组件）

| # | 类型 | 内容 | 涉及范围 | 说明 |
|---|------|------|---------|------|
| S1 | 跨组件改造 | **展示类组件统一 `dataSource` 注入机制** | `SlotDispatch` + 所有展示类组件（table/chart/calendar/timeline/timetable/card-list/kanban/tree/map/flashcards/quiz/stat-tile） | 当前仅 `data-table` 和 `timetable` 支持 `dataSource` 异步拉取，其余展示组件只读静态 config。统一改造：`ComponentDescriptor.dataSource` → renderer 归一化处理（`_resolveData()` / `_resolveRows()` → 自动注入组件 config）。实现后 `calendar` 可从数据源拉取实时考试事件、`chart` 可拉取动态数据系列、`stat-tile` 可拉取动态指标等 |
| S2 | 新组件 | **`pdf-viewer`** | `CompositeView` switch + HTML `_renderPdfViewer` + widgets/ | 内嵌 PDF 预览/翻页。解决 FAIL2CLONE training-plans PDF 详情需求。可用 `flutter_pdf_viewer` 或 PDF.js 嵌入 |
| S3 | 新组件 | **`scanner`** | `CompositeView` switch + HTML `_renderScanner` + widgets/ | 扫码/OCR 识别。校园场景（一卡通/教室码/图书 ISBN），HTML 侧可用 `BarcodeDetector` API，Dart 侧可用 `mobile_scanner` |
| S4 | 新插件 | **新一代展示大厅 ×2**（Dart + HTML） | `plugins/showcase-v3/` + `plugins/showcase-v3-html/` | 覆盖全部 43 个组件类型 + manifest 全层级可选参数（theme/layout.preset/layout.features/slot.events/page.process/action/workspace/dataSource）。所有数据使用 **Evergreen 项目自身宣传素材**：产品 Logo、功能截图、架构视频、开发团队信息、版本迭代时间线、用户增长 chart、GitHub Star 统计等。**边展示组件能力，边打项目广告**。禁止任何虚构/硬编码内容——每一行数据都是真实可核的项目信息。预期效果：一篇 markdown 不够看的，打开展示大厅直接感受完整产品力 |

### 14.3 执行里程碑

| 里程碑 | 目标 | 预计完成后 |
|--------|------|-----------|
| **M1: 补齐** | 第一章 ⚠️/❌ 标记的组件全部升级为 ✅（Dart + HTML 双端） | 43 个具名组件全量实现，零硬编码 |
| **M2: 改造** | S1 统一 dataSource 注入落地 | 所有展示类组件均可对接真实数据源，calendar/chart 等不再受限于静态 config |
| **M3: 扩展** | S2 `pdf-viewer` + S3 `scanner` 完成 | 组件类型从 43 → 45，覆盖 PDF 浏览和扫码场景 |
| **M4: 验收** | S4 新一代展示大厅（Dart + HTML）完成 | 全部组件 + 全部参数正交覆盖，真实项目宣传数据，产出一份「打开即信服」的产品演示 |

> **当前状态**：M0（基线）— 43 组件中 28 已实现、15 占位；7 个 HTML 硬编码；3 条 FAIL2CLONE 中 2 条已纳入本计划。
