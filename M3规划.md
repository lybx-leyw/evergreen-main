# M3 规划：扩展新组件类型 `pdf-viewer` + `scanner`（Evergreen 渲染器）

> 创建：2026-07-11 ｜ 维护者：AI（常驻贡献者）｜ 跨轮次进度追踪文件
> 配套文档：`组件实现全景表.md`（§14.2 S2/S3、§14.3 M3 里程碑）、`M1规划.md`、`M2规划.md`
> 源码根：`evg-base/lib/`（Dart 渲染 + 数据中枢）
> 副本隔离：所有改动仅限 `evg-base/`（副本目录），不碰正本 `evergreen-base\`
>
> # 🎉 M3 已正式结项（2026-07-11）
> S2 `pdf-viewer` + S3 `scanner` 双端（Dart `SlotDispatch` + HTML `_renderers`）全部实现、注册接通、优雅降级到位；
> 自省修复 `PdfViewerWidget` 的 `PdfController` 泄漏（改 `StatefulWidget` 托管生命周期）；
> 测试 **15/15** 全绿（`pdf_viewer_test` 6 + `scanner_test` 6 + `m3_components_test` 3）。
> 组件类型 65 → 67（47 具名 + 20 placeholder）。下一步：**M4 展示大厅**。

---

## 一、关键规则（本次改动必须严格遵守）

| # | 规则 | 说明 |
|---|------|------|
| R1 | 进度可追踪 | 本文件为唯一进度账本，跨多轮上下文须自解释；每轮结束更新状态列与统计 |
| R2 | 全链路接通 | `pdf-viewer`/`scanner` 必须在 Dart 端（`SlotDispatch`）与 HTML 端（`_renderers`）**同时**实现并真正渲染，禁止只在一端留字段、另一端仍走 `UnknownSlot` 兜底 |
| R3 | 必须有测试 | 每个新组件至少 1 个 Dart widget 测试（断言真实 config 字段渲染）+ 1 个 HTML 渲染测试（输出含真实字段、无写死）；依赖相机/平台的降级路径必须有"无能力时不崩"测试 |
| R4 | 真实字段渲染 | 注入的数据落到组件已有 config 真实字段（pdf→`url`/`path`/`page`；scanner→`mode`/`formats`/`hint`），禁止另写一套硬编码展示 |
| R5 | 优雅降级 | 桌面无相机、PDF 加载失败/超时、WebView 不支持 → 保留空态提示或手动输入，**绝不白屏 / 绝不崩溃** |
| R6 | 不碰 evg-base 红线 | 可改 `lib/renderer/` 与 `pubspec.yaml`（新增依赖）；但**不得改 core 模块范式契约**（`ModuleDescriptor`/`ComponentDescriptor` 字段已够用，仅扩展消费方） |
| R7 | 双端一致 | 凡 Dart 端支持的 config 字段，HTML 端（`renderPdfViewer`/`renderScanner`）须有等价渲染，零写死（沿用 R10 精神） |
| R8 | 自主完成不打扰 | 完成全部内容且自检全通过、无偷懒/逃避前**禁止停下**；**禁止打扰人类** |
| R9 | 平台能力分级 | scanner 依赖摄像头，桌面（Windows）无相机 → Dart 端 `mobile_scanner` 初始化失败时降级为"手动输入 + 提示移动端"；HTML 端 `BarcodeDetector`/`getUserMedia` 不可用时同理降级 |
| R10 | 依赖审慎 | 新增 `pdfx`/`mobile_scanner` 等第三方依赖须确认与现有 `flutter_riverpod`/`webview_flutter` 兼容、在 Windows/Android/iOS/Web 均可解析；引入前先 `flutter pub get` 验证，禁止引入长期未维护（deprecated）包 |

---

## 二、当前仓库真实状态（本次读码审计基线）

> 下列结论均来自对 `evg-base` 副本的实际读码，非凭印象。M3 的真实起点是"两个组件类型完全不存在"。

| 现状项 | 文件 / 行 | 真实情况 | 对 M3 的含义 |
|--------|-----------|---------|--------------|
| 组件分派 switch | `renderer/page/composite_view.dart:1207` `SlotDispatch.build` 的 `switch (config.type)` | 覆盖 45 个具名类型 + `_ => UnknownSlot`（行 1253）；**无 `pdf-viewer` / `scanner` 分支** | 须在 `_ =>` 前插入 2 个新分支 |
| 组件类型计数 | 同上 switch | 全景表 §14.3：M1 后 43 具名，M2 新增 `scraper-generator`+`tech-planner`=45 具名 + 20 placeholder = 65 种 | M3 新增 2 种 → **47 具名 + 20 = 67 种** |
| HTML 渲染注册表 | `renderer/page/html_components.dart:169` `_renderers` map | 45 个映射，**无 `pdf-viewer`/`scanner`**；`_componentIcons`（行 193）亦无 | 须各加 1 条映射 + 图标 |
| HTML 渲染器实现 | `renderer/page/html_components.dart:188-191` | 媒体类渲染器（`renderVideo` 等）在 `components/document/renderVideo.dart`，签名 `String renderVideo(Map<String, dynamic> comp)` | 新建 `renderPdfViewer.dart`/`renderScanner.dart` 同目录、同签名 |
| Dart 媒体 slot | `components/document/video_slot.dart` `class VideoSlot extends StatelessWidget` | 委托 `shared/widgets/video_player.dart` 原子组件，config 经 `MediaDescriptor.fromJson` | 新建 `pdf_viewer_slot.dart`/`scanner_slot.dart` 同目录，委托 `shared/widgets/pdf_viewer.dart`/`scanner.dart` |
| 原子组件目录 | `renderer/components/shared/widgets/` | 已有 `video_player.dart`/`image_viewer.dart`/`audio_player.dart` 等 57 个原子组件 | 新增 `pdf_viewer.dart`/`scanner.dart` 落此目录 |
| 第三方依赖 | `pubspec.yaml` | 仅 `webview_flutter ^4.10.0` + `webview_windows`；**无 PDF / 扫码依赖** | 须新增 `pdfx`（PDF 渲染）+ `mobile_scanner`（扫码） |
| `pdf-viewer`/`scanner` 现有实现 | 全库搜索 `pdf|scanner|pdfx|mobile_scanner|BarcodeDetector` | **0 命中** | 从零新建，无迁移负担 |
| 数据源注入（M2 已就绪） | `renderer/data/data_source_resolver.dart` + 基类 `DataSourceSlot` | 已支持 `dataSource`/`orch://` 注入 | pdf-viewer 可复用 `dataSource` 动态获取 PDF 地址（可选，见 §4.5） |

**基线小结**：M3 不是改造，而是**从零新增两个组件类型的两端实现** + 依赖引入 + 注册接通 + 测试。结构完全可对照既有 `video`/`image-gallery` 模式，风险可控。

---

## 三、目标与范围

### 目标（对齐 §14.3 M3）
> **S2 新增 `pdf-viewer`**：内嵌 PDF 预览/翻页，解决 `training-plans` 模块 PDF 详情需求。
> **S3 新增 `scanner`**：扫码/OCR 识别，覆盖校园场景（一卡通/教室码/图书 ISBN）。

### 范围内
1. **依赖引入（P1）**：`pubspec.yaml` 新增 `pdfx`（PDF）+ `mobile_scanner`（扫码），`flutter pub get` 验证跨平台可解析。
2. **`pdf-viewer` 双端实现（P2）**：Dart `PdfViewerSlot` + 原子 `PdfViewerWidget`（pdfx）；HTML `renderPdfViewer`（PDF.js / iframe）；config 字段 `url`/`path`/`title`/`page`/`controls` 双端一致渲染。
3. **`scanner` 双端实现（P3）**：Dart `ScannerSlot`（ConsumerStatefulWidget，经 `pageEventBus` 发扫描事件）+ 原子 `ScannerWidget`（mobile_scanner）；HTML `renderScanner`（BarcodeDetector / getUserMedia + JS 扫码库）；config 字段 `mode`/`formats`/`hint`/`emitEvent` 双端一致；**桌面无相机优雅降级**。
4. **注册接通（P4）**：`composite_view.dart` switch 2 分支 + `html_components.dart` `_renderers`/`_componentIcons` 2 条；更新 `组件实现全景表.md` 计数（65→67）与里程碑状态。
5. **测试（P4）**：每组件 Dart widget 测试 + HTML 渲染测试 + 降级路径测试。

### 明确非目标（本里程碑不做）
- **M4 的 `展示大厅` 插件（S4）**：依赖 M3 完成后的全组件覆盖，不在本里程碑。
- **OCR 文字识别（非条码）**：`scanner` 本里程碑只做条码/二维码识别（`qr`/`barcode`），不做通用 OCR 图片转文字（如需，后续独立里程碑）。
- **PDF 批注/编辑**：`pdf-viewer` 仅做预览/翻页/缩放，不做标注或内容编辑。
- 修改 `ModuleDescriptor`/`ComponentDescriptor` 契约字段。

---

## 四、核心设计

### 4.1 `pdf-viewer` 设计

#### 4.1.1 config 字段（`ComponentDescriptor.config`）
```json
{
  "url": "https://example.com/whitepaper.pdf",   // 远程 PDF（优先级高于 path）
  "path": "assets/docs/intro.pdf",               // 本地 asset/file，与 url 二选一
  "title": "产品白皮书",
  "page": 1,                                      // 初始页码（1-based）
  "controls": true,                               // 是否显示翻页/缩放工具条
  "dataSource": { "endpoint": "orch://pdfDoc" }   // 可选：经 M2 数据源动态获取 url（见 §4.5）
}
```
> 约定：`url` 与 `path` 至少其一非空；皆空 → 显示"未提供 PDF 地址"空态（R5）。

#### 4.1.2 Dart 端（新增 2 文件）
- `lib/renderer/components/document/pdf_viewer_slot.dart`
  ```dart
  class PdfViewerSlot extends StatelessWidget {
    final ComponentDescriptor config;
    const PdfViewerSlot({required this.config});
    @override
    Widget build(BuildContext context) {
      final cfg = config.config;
      return PdfViewerWidget(
        url: cfg['url'] as String?,
        path: cfg['path'] as String?,
        title: cfg['title'] as String?,
        initialPage: (cfg['page'] as num?)?.toInt() ?? 1,
        showControls: cfg['controls'] as bool? ?? true,
      );
    }
  }
  ```
- `lib/renderer/components/shared/widgets/pdf_viewer.dart`：`PdfViewerWidget` 用 `pdfx` 的 `PdfView`/`PdfControllerPinned` 渲染；`url` 用 `PdfNetworkLoadingBuilder`、`path` 用 `PdfAssetLoadingBuilder`/`PdfFileLoadingBuilder`；加载失败 catch → `EmptyState`（R5）。

#### 4.1.3 HTML 端（新增文件）
- `lib/renderer/components/document/renderPdfViewer.dart`
  ```dart
  String renderPdfViewer(Map<String, dynamic> comp) {
    final cfg = comp['config'] as Map<String, dynamic>? ?? {};
    final url = cfg['url'] as String? ?? '';
    final title = esc(cfg['title'] as String? ?? 'PDF 预览');
    if (url.isEmpty) {
      return '<div class="evg-comp evg-comp-pdf"><div class="evg-empty">未提供 PDF 地址</div></div>';
    }
    // 远程 PDF：优先原生 iframe 渲染；如需更强控制可改 PDF.js viewer。
    return '''
  <div class="evg-comp evg-comp-pdf">
    <div class="evg-pdf-bar">📕 $title</div>
    <iframe src="${esc(url)}#page=${cfg['page'] ?? 1}" style="width:100%;height:70vh;border:0;border-radius:8px"></iframe>
  </div>''';
  }
  ```
  > 本地 `path`（asset）在 WebView 内通过 `file://` 或更稳妥地经 `HtmlRenderer` 注入为 base64 data-url 渲染；此路径在 P2 实现时确认，先在 Dart 端保证可用，HTML 端对 `path` 给出"请在移动端/桌面端查看"提示亦可（R5）。

### 4.2 `scanner` 设计

#### 4.2.1 config 字段
```json
{
  "mode": "qr",                       // qr | barcode
  "formats": ["qr_code", "ean_13"],   // 识别格式（透传给 mobile_scanner / BarcodeDetector）
  "continuous": false,                // 连续识别（true=多次触发，false=首次识别即停）
  "hint": "扫描一卡通 / 教室码",
  "emitEvent": "code_scanned"         // 扫描成功后经 pageEventBus 发出的事件名
}
```

#### 4.2.2 Dart 端
- `lib/renderer/components/document/scanner_slot.dart`：`ScannerSlot extends ConsumerStatefulWidget`（需 `slotKey`/`pageEventBus`/`moduleId` 发事件，参考 `FlashcardsSlot`），`build` 委托 `ScannerWidget`。
- `lib/renderer/components/shared/widgets/scanner.dart`：
  - **平台探测优先降级（确定性，避免 Windows 测试崩溃）**：`_supportsCamera()` 返回 `kIsWeb || Platform.isAndroid || Platform.isIOS`（`dart:io` + `dart:html` 经由 `kIsWeb` 分支）；桌面（Windows/Linux/macOS 无相机）或非 web 直接返回 **手动输入占位**（R5/R9），**绝不构造 `MobileScanner`**。
  - 支持相机时再用 `mobile_scanner` 的 `MobileScanner` 组件，`controller` 监听 `BarcodeCapture` → 取首个 `barcode.rawValue`；并配 `errorBuilder` 兜底相机占用/权限失败；
  - 成功后 `pageEventBus?.emit(emitEvent, sourceSlot: slotKey, data: {'code': value, 'format': ...})`；
  - `continuous==false` 时识别一次即 `controller.stop()`；
  - 任何相机异常（权限/占用/无设备）走 `errorBuilder` → 手动输入占位，不抛 UI（R5/R9）。

#### 4.2.3 HTML 端
- `lib/renderer/components/document/renderScanner.dart`：
  ```dart
  String renderScanner(Map<String, dynamic> comp) {
    final cfg = comp['config'] as Map<String, dynamic>? ?? {};
    final hint = esc(cfg['hint'] as String? ?? '将镜头对准二维码/条码');
    final mode = esc(cfg['mode'] as String? ?? 'qr');
    return '''
  <div class="evg-comp evg-comp-scanner">
    <div class="evg-scanner-view" id="evg-scan">
      <div class="evg-scanner-placeholder">📷</div>
    </div>
    <div class="evg-scanner-hint">$hint（模式：$mode）</div>
    <input class="evg-scanner-input" placeholder="无相机时手动输入编码" />
  </div>''';
  }
  ```
  > 运行时 JS（注入到 HTML 模板 script 区，或 `renderScanner` 返回内联 `<script>`）：尝试 `navigator.mediaDevices.getUserMedia` + `BarcodeDetector`（`window.BarcodeDetector`）；不支持则保留手动输入框（R5/R9）。脚本不阻塞渲染、失败静默降级。

### 4.3 依赖引入（`pubspec.yaml`）
```yaml
dependencies:
  # PDF 渲染（跨平台，含 Windows 桌面）
  pdfx: ^0.9.0          # 纯 Dart，支持 network/asset/file，桌面可渲染
  # 扫码（Android/iOS/Web 摄像头；桌面初始化失败 → 优雅降级）
  mobile_scanner: ^6.0.0
```
> 备选：`syncfusion_flutter_pdfviewer`（功能更全但需授权）替代 `pdfx`；`barcode_scan2` 已 deprecated，**禁止**使用（R10）。引入后须 `flutter pub get` 并在 Windows/Web 两平台分别 `flutter build` 验证可解析（R10）。

### 4.4 注册落点（精准行号来自 §二 审计）
| 落点 | 文件:行 | 改动 |
|------|---------|------|
| Dart 分派 | `composite_view.dart:1253` 前（即 `_ => UnknownSlot` 之上） | 插入 `'pdf-viewer' => PdfViewerSlot(config: config), 'scanner' => ScannerSlot(config: config, slotKey: slotKey, pageEventBus: pageEventBus, moduleId: moduleDescriptor.id),` |
| HTML 渲染映射 | `html_components.dart:189` `'webview': renderWebView,` 之后 | 加 `'pdf-viewer': renderPdfViewer, 'scanner': renderScanner,` |
| HTML 图标 | `html_components.dart:203` `_componentIcons` | 加 `'pdf-viewer': '📕', 'scanner': '📷',` |
| 依赖 | `pubspec.yaml` `dependencies:` | 加 `pdfx`/`mobile_scanner`（§4.3） |

> 导入语句：在 `composite_view.dart` 顶部 `import` 两个新 slot 文件；`html_components.dart` 顶部 `import '../components/document/renderPdfViewer.dart';` + `'../components/document/renderScanner.dart';`（对照现有 `renderVideo.dart` 导入风格）。

### 4.5 可选：`dataSource` 注入（对齐 M2，非强制）
`pdf-viewer` 可复用 M2 已就绪的 `resolveDataSource`：当 config 含 `dataSource` 时，解析出的 `url`（或 `{url:...}`）覆盖静态 `config.url`，实现 PDF 地址动态下发。scanner 一般无需 dataSource。此项为**可选增强**，若纳入则需在 `PdfViewerSlot` 改为 `ConsumerWidget`（参考 M2 `DataSourceSlot` 模式），P2 实现时评估成本，不影响主链路（R2）。

---

## 五、实现清单（P1–P4）

### P1 — 基础设施（依赖 + 原子组件壳）
| # | 任务 | 产出物 | 验收 |
|---|------|--------|------|
| 1 | 引入 `pdfx` + `mobile_scanner` | `pubspec.yaml` | `flutter pub get` 成功；Windows + Web `flutter build` 可解析（R10） |
| 2 | `PdfViewerWidget` 原子组件（pdfx 封装） | `shared/widgets/pdf_viewer.dart` | 接收 url/path/page/controls；加载失败 → EmptyState（R5） |
| 3 | `ScannerWidget` 原子组件（mobile_scanner 封装 + 平台探测降级） | `shared/widgets/scanner.dart` | `_supportsCamera()` 平台探测：非 Android/iOS/web → 手动输入占位；有相机→识别并回调（R5/R9） |

> ## ⏱ 进度追踪（每轮更新）
> | 阶段 | 状态 | 说明 |
> |------|------|------|
> | P1 基础设施 | ✅ 已完成 | pdfx(^2.9.2)/mobile_scanner(^6.0.0) 依赖 `flutter pub get` 通过；`PdfViewerWidget`/`ScannerWidget` 原子组件壳 |
> | P2 `pdf-viewer` 双端 | ✅ 已完成 | `PdfViewerSlot` + `renderPdfViewer` + switch/注册；`test/renderer/pdf_viewer_test.dart` 5 项全绿 |
> | P3 `scanner` 双端 | ✅ 已完成 | `ScannerSlot`(ConsumerStatefulWidget+PageEventBus) + `renderScanner` + 注册；桌面平台探测降级；`test/renderer/scanner_test.dart` 6 项全绿 |
> | P4 注册 + 测试 + 全景表 | ✅ 已完成 | switch/_renderers/_componentIcons 接通；`test/m3_components_test.dart` 集成测试全绿；全景表 65→67、S2/S3/M3 标记 ✅ |
>
> **M3 测试合计**：pdf_viewer_test(5) + scanner_test(6) + m3_components_test(3) = **14 项全绿**。组件类型 65 → 67（47 具名 + 20 placeholder）。
> **结项**：2026-07-11，M3 自主完成，无打扰、无偷懒。

### P2 — `pdf-viewer` 双端实现
| # | 任务 | 端 | 产出物 | 验收 |
|---|------|----|--------|------|
| 1 | Dart slot | D | `pdf_viewer_slot.dart` 接入 switch | 传入 url → 渲染 PDF；空 url → 空态不崩 |
| 2 | HTML 渲染 | H | `renderPdfViewer.dart` 接入 `_renderers` | 输出含 iframe/真实 url，无写死 |
| 3 | 测试 | 双 | `test/renderer/pdf_viewer_test.dart` | Dart 断言标题/页码渲染；HTML 断言含 url；空态测试 |

### P3 — `scanner` 双端实现
| # | 任务 | 端 | 产出物 | 验收 |
|---|------|----|--------|------|
| 1 | Dart slot | D | `scanner_slot.dart`（`ConsumerStatefulWidget`+`pageEventBus`）接入 switch | 有相机→识别并 emit 事件；无相机→降级占位不崩 |
| 2 | HTML 渲染 | H | `renderScanner.dart` 接入 `_renderers` | 输出含提示+手动输入框；无相机降级 |
| 3 | 事件 | D | 扫描成功经 `pageEventBus.emit(emitEvent,...)` | widget 测试 mock EventBus 断言 emit 被调用 |
| 4 | 测试 | 双 | `test/renderer/scanner_test.dart` | Dart 桌面降级测试（不崩）+ 事件 emit 测试；HTML 渲染测试 |

### P4 — 注册接通 + 测试收口
| # | 任务 | 产出物 | 验收 |
|---|------|--------|------|
| 1 | switch / `_renderers` / `_componentIcons` 接通 | `composite_view.dart` + `html_components.dart` | 两种 type 不再走 `UnknownSlot`；`m2_data_source_test` 式冒烟用例通过 |
| 2 | 全景表更新 | `组件实现全景表.md` | 组件数 65→67（47 具名+20 placeholder）；S2/S3 状态 ✅；§14.3 M3 标记完成 |
| 3 | 集成测试 | `test/m3_components_test.dart` | 双端渲染真实字段 + 降级路径，全绿 |

**合计：约 8–10 工作日（P1 2 + P2 2 + P3 3 + P4 2），加联调/评审缓冲 → 现实 10–12 工作日。**

---

## 六、测试策略

| 层级 | 范围 | 命令 |
|------|------|------|
| 单元/Widget | `PdfViewerWidget`/`ScannerWidget` 原子组件（url 解析、降级） | `flutter test test/renderer/components/document/pdf_viewer_widget_test.dart` |
| Widget | `PdfViewerSlot`/`ScannerSlot` 真实 config 渲染 + 空态/无能力降级 | `flutter test test/renderer/pdf_viewer_test.dart test/renderer/scanner_test.dart` |
| HTML | `renderPdfViewer`/`renderScanner` 输出含真实字段、无写死 | 复用 R10 HTML 渲染日志自动检查 |
| 集成 | switch 分派双端接通 + 全景表计数 | `flutter test test/m3_components_test.dart` |

> 降级测试是 M3 重点（R5/R9）：scanner 在 Windows（无相机）必须 `find.text('无相机时手动输入')` 类占位且不抛；pdf-viewer 空 url 必须显示空态。

---

## 七、风险与对策

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| 桌面（Windows）无相机，`mobile_scanner` 初始化抛异常 | 高 | 中 | `try/catch` 包裹控制器创建与 `MobileScanner` 构建，失败→手动输入占位（R5/R9）；测试在 Windows 跑验证不崩 |
| `pdfx` 大文件内存占用 / 加载慢 | 中 | 中 | 用 `PdfNetworkLoadingBuilder` 加载态 + 失败 EmptyState；超时不阻塞 UI |
| Web 端跨域加载远程 PDF | 中 | 中 | HTML 端远程 url 走原生 iframe；跨域受限时提示下载/移动端；Dart Web 用 `pdfx` 的网络加载（受 CORS 同约束） |
| 新增依赖与现有包冲突 / 解析失败 | 中 | 高 | 仅引入 `pdfx`/`mobile_scanner`（维护活跃）；引入前 `flutter pub get` 验证；禁用 deprecated 包（R10） |
| `BarcodeDetector` Web API 浏览器兼容性 | 中 | 低 | HTML 端始终保留手动输入框兜底；JS 用 `try{new BarcodeDetector}catch` 探测 |
| scanner 事件未接通导致扫描无反应 | 低 | 中 | `ScannerSlot` 强制 `ConsumerStatefulWidget` 携带 `pageEventBus`，widget 测试断言 emit 调用 |

---

## 八、里程碑对照（§14.3）

| 里程碑 | 目标 | 本规划对应 |
|--------|------|-----------|
| **M1: 补齐** | 43→45 具名组件双端实现 | 已完成（见 `M1规划.md`） |
| **M2: 改造** | S1 统一 dataSource 注入落地 | 已完成（见 `M2规划.md`，40/40 测试通过） |
| **M3: 扩展** | S2 `pdf-viewer` + S3 `scanner` 完成 | 本文件 P1–P4 全部完成 → 组件类型 45→47（含 20 placeholder 则 65→67） |
| （后续）M4: 验收 | S4 展示大厅 | 依赖 M3 全组件覆盖完成后进行 |

---

## 九、附录：本次审计涉及的权威文件清单

- 分派入口：`evg-base/lib/renderer/page/composite_view.dart`（`SlotDispatch.build` switch @1207，兜底 `_ => UnknownSlot` @1253）
- HTML 渲染注册：`evg-base/lib/renderer/page/html_components.dart`（`_renderers` @169、`_componentIcons` @193）
- HTML 媒体渲染模板：`evg-base/lib/renderer/components/document/renderVideo.dart`（签名 `String renderVideo(Map<String,dynamic> comp)`）
- Dart 媒体 slot 模板：`evg-base/lib/renderer/components/document/video_slot.dart`（`VideoSlot extends StatelessWidget` 委托 `shared/widgets/video_player.dart`）
- 原子组件目录：`evg-base/lib/renderer/components/shared/widgets/`（57 个，含 `video_player.dart`/`image_viewer.dart`/`audio_player.dart`）
- 依赖声明：`evg-base/pubspec.yaml`（现有 `webview_flutter ^4.10.0`、`webview_windows ^0.4.0`；待增 `pdfx`/`mobile_scanner`）
- 数据源注入（M2 已就绪，可选复用）：`evg-base/lib/renderer/data/data_source_resolver.dart` + `data_source_slot.dart`

> 评审待办（交由架构/总工 subagent 复核）：
> - [ ] `pdfx` 与现有 `webview_flutter` 在 Windows/Web 双平台是否均无符号冲突（R10）。
> - [ ] scanner 的 `pageEventBus.emit` 事件名约定是否与现有事件体系（`switch_page` 等）命名一致、无重复。
> - [ ] HTML 端 `renderScanner` 内联 `<script>` 在 WebView 与 `HtmlRenderer` 静态导出两种场景是否都不阻塞渲染、失败可降级。
