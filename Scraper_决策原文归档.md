# Scraper V2.0 决策原文归档（Decision Log · 逐字引用用户回答）

> 目的：**用户通过 ask 工具给出的每一条回答，逐字归档，映射到需求编号 + 落实位置**，防止转述丢失语义。本文档是 `Scraper_V2.0_全量重构计划.md` §一 需求清单的**权威来源**——需求清单条目若有与本归档冲突，以本归档为准。
> 标注：`✅ 原文已落实` / `⚠️ 此前转述有偏差（见"偏差修正"）` / `🔍 需进一步确认`

---

## 第 1 轮 ask（5 问 · 实施范围与探索决策）

### Q1. 实施范围与顺序
**问题**：四项改造的实施范围与顺序？
**用户回答（custom）**：
> "全部需要完成，但不是一次性完成，先写全量改造的PLAN，分几个Phase完工"

**映射**：需求 E1 ✅（分 Phase 完工，每 Phase 独立可验证）
**落实**：`Scraper_V2.0_全量重构计划.md` §三（4 Phase）+ §十一（交付顺序）

### Q2. Trace 视图接入范围
**问题**：Trace 视图的接入范围？
**用户回答（选中）**："组件共享化，先只接 scraper (Recommended)"
**映射**：需求 C5 ✅
**落实**：Phase 3，`renderer/components/shared/trace/` 共享 + scraper 先接入

### Q3. 探索守卫默认值
**问题**：AI 探索的守卫上限默认值？
**用户回答（选中）**："同域 + 20 页 + 50 请求 + 1s 节流 (Recommended)"
**映射**：需求 D7 ✅
**落实**：Phase 4 `ExploreWorkflow` 上限/节流计数（可配置）

### Q4. 批量注册形态
**问题**：批量注册的产物形态？
**用户回答（选中）**："每源一个插件目录 data-{name} (Recommended)"
**映射**：需求 D8 ✅
**落实**：Phase 4 building 阶段，逐源 `data-{name}`

### Q5. 探索模式触发方式
**问题**：探索机制的触发方式？
**用户回答（custom）**：
> "探索模式和现在的只爬取目标数据是两个模式，有不同的工作流和harness约束工作，但理论上应该仅支持手动选择模式"

**映射**：需求 D9 ✅
**落实**：双模式（定向 / 探索），画板内手动选择（Phase 2 画板 + Phase 4 探索落地）
**关键语义**：两种模式有**不同工作流**和**不同 harness 约束**——不是同一工作流加开关，是两个独立状态机 + 两套守卫约束。

---

## 第 2 轮 ask（5 问 · 守卫规则决策）

### Q1. 假数据检测严格度
**问题**：硬编码假数据/无网络请求的静态检测命中后怎么处理？
**用户回答（custom）**：
> "warning放行 + 回灌修正，不阻断，但是workflow会卡AI，让AI没办法明明是假数据却成功推到下一步"

**映射**：需求 A5 ✅
**落实**：Phase 1 G5 门禁（`markDone()` 检查 guardFlags → 弹窗 → 放行/拒绝）
**关键语义**：① warning 不阻断**执行**（AI 能看到真实结果）；② 但 workflow 在**推进下一步**时卡住（软警告 + 硬门禁组合）。

### Q2. import 白名单严格度
**问题**：Python 代码 import 白名单的严格度？
**用户回答（选中）**："严格 stdlib+requests (Recommended)"
**映射**：需求 A6 ✅
**落实**：Phase 1 `lintScraperCode` import 白名单

### Q3. 终端命令守卫策略
**问题**：run_terminal_command 的白名单策略？
**用户回答（选中）**："白名单外弹窗确认"
**映射**：需求 A7 ✅（黑名单硬拒 / 白名单自动 / 其余弹窗确认——弹窗选项描述即此三态）
**落实**：Phase 1 `ScraperGate.pendingCallback`

### Q4. 凭证 key 校验
**问题**：save_credential 的 key 命名校验？
**用户回答（custom）**：
> "允许任何key，我们也不想要我们的设置里出现一大堆SCRAPER，但是key要写清楚，最好包含功能简写。"

**映射**：需求 A8 ✅
**落实**：Phase 1 `validateCredentialArgs`（不强制前缀）+ prompt 引导功能简写
**⚠️ 偏差修正**：此前未记录**动机**"不想要设置里出现一大堆 SCRAPER"——这是不强制前缀的原因，需写入需求注释。

### Q5. write_file 缺失处理
**问题**：prompt 引导的 write_file 工具不存在，怎么处理？
**用户回答（选中）**："仅修 prompt (Recommended)"
**映射**：需求 A9 ✅
**落实**：Phase 1 prompt 修正（write_file → run_python_scraper 写盘）

---

## 第 3 轮 ask（3 问 · workflow 机制 + 轮次 + 弹窗时机）

### Q1. workflow 覆盖与补充场景
**问题**：workflow × harness 图与阻断/回退矩阵是否覆盖你要的机制？
**用户回答（custom，长文）**：
> "补充一个场景，当AI全部pass之后人工一看——觉得一点不ok，这时候就要开启第二轮会话，也就是当前文已经完成用户需求的MVP，只是处理用户反馈进行debug或者优化改进时，同样走你图中画的 [debugging] 路线，而非从头抓包的那个工作流。然后是我们的AI走workflow但不是无人值守的，AI随时可以ask我们，我们也可以随时打断AI补充要求，整个workflow不变。AI可以自行选择无条件回退到workflow之前已完成的步骤，但不能不通过harness验收机制就step到下一步。就是，我们要做的是和正常agent没太大区别，只是做了特定场景的workflow×harness强化的Agent，以此实现超级性能。理解吗？"

**映射**：
- 第二轮会话（MVP 已满足 + 反馈驱动 debug/优化，走 debugging 路线）→ 需求 A16
- AI 随时 ask → A11
- 用户随时打断补充要求，workflow 不变 → A17
- AI 无条件回退到已完成步骤 → A14
- 不能不经验收 step 到下一步 → A25
- "workflow×harness 强化 Agent"本质 → A25
**落实**：Phase 1（workflow 工程化 + AskTool + 停止按钮 + 回退 + 验收门）
**⚠️ 偏差修正**：此前 A16 未写清"MVP 已满足需求，第二轮是**反馈驱动的 debug/优化改进**（非抓包）"这一前置语义；且"整个workflow不变"（打断不改 workflow 结构）应显式记录。

### Q2. 调试轮次与日志快照
**问题**：R5 不消耗调试轮次认可吗？
**用户回答（custom，长文）**：
> "认可R5不消耗轮次。而且当前调试轮次本质上就是一个防止AI自循环的guard而已，我们机制要改一下，不需要 5 轮调试预算，改成连续3轮调适后仍失败，此时就warning发给AI，告知AI请使用网页探索的能力去探索用户未暴露的相关接口。然后就是用户操作完一次后，其操作的日志必须全量快照保存起来，AI随时可以调用工具来阅读日志，用户确认操作完毕后日志不再更新——即便用户在操作。若AIask 用户指出需要重新走一遍之类的，那就触发确认框，同意后web_view回到首页，重启日志抓取和确认流程"

**映射**：
- 调试轮次 = 防 AI 自循环的 guard（动机）→ A15
- 连续 3 轮失败 → warning（提示用网页探索探索未暴露接口）→ A15
- 日志全量快照保存 + **AI 随时可调用工具阅读日志** → A18 + **新增 A26**
- 用户确认操作完毕后日志不再更新（即便用户在操作）→ A18（锁定 WebView）
- **AI ask 指出需重新走一遍 → 触发确认框** → A18 修正
- 同意后 web_view 回首页，重启日志抓取和确认流程 → A18
**落实**：Phase 1 workflow 工程化
**⚠️ 偏差修正（重要）**：
1. 此前**未记录"AI 随时可以调用工具来阅读日志"**——需要独立的**日志快照阅读工具**（如 `read_request_snapshot`），不是只有 UI 面板可见！→ 新增需求 A26
2. 此前 A18 只写"重抓需确认框"，**未写清确认框可由"AI ask 用户指出需要重新走一遍"触发**（不只有用户手动点）→ 修正 A18
3. 此前未记录调试轮次的设计动机"防 AI 自循环的 guard" → 修正 A15 注释

### Q3. G5 弹窗时机
**问题**：G5 假数据门禁的弹窗时机？
**用户回答（custom）**：
> "AI澄清后即弹。注意，我们刚刚提出了大量之前未详细说明的关键决策要点，所以你可能还需要重新打磨PLAN，先不动手。可以多ask me一些问题以更好理解我的需求"

**映射**：需求 A10 ✅（AI 澄清后即弹，展示守卫原因 + AI 澄清文本，用户裁决）
**落实**：Phase 1 G5 门禁

---

## 第 4 轮 ask（8 问 · 快照/第二轮/回退/轮次/ask 工具）

### Q1. 日志快照时机
**问题**：日志快照冻结的触发时机？
**用户回答（选中）**："新增显式「确认操作完毕」按钮 (Recommended)"
**映射**：需求 A18 ✅
**落实**：Phase 1 `confirmCaptureDone()` 按钮

### Q2. 冻结后行为
**问题**：快照冻结后用户还能操作 WebView 吗？
**用户回答（选中）**："锁定 WebView"
**映射**：需求 A18 ✅（冻结 → 锁定 WebView）
**落实**：Phase 1

### Q3. 第二轮会话入口
**问题**：MVP 完成（done）后用户觉得不 OK，怎么开启第二轮？
**用户回答（选中）**："对话内反馈触发 (Recommended)"
**映射**：需求 A16 ✅
**落实**：Phase 1 `feedbackTriggered()` 检测对话反馈 → refining

### Q4. 迭代轮次上限
**问题**：第二轮及后续迭代的轮次策略？
**用户回答（选中）**："不设硬上限，仅展示轮次 (Recommended)"
**映射**：需求 A19 ✅
**落实**：Phase 1（refining 循环轮次展示）

### Q5. AI 回退规则
**问题**："AI 可无条件回退到已完成步骤"的具体规则？
**用户回答（选中）**："自主回退+重过验收 (Recommended)"
**映射**：需求 A14 ✅
**落实**：Phase 1 `rollbackTo()` + 回退后重过验收

### Q6. 3 轮 warning 后行为
**问题**：连续 3 轮调试失败 warning 之后的行为？
**用户回答（选中）**："warning 后继续，反复提醒 (Recommended)"
**映射**：需求 A15 ✅
**落实**：Phase 1（每轮失败重申 warning）

### Q7. 探索能力衔接
**问题**：调试 warning 提到的"网页探索能力"是 Phase 4 的探索，Phase 1 怎么衔接？
**用户回答（选中）**："Phase 1 只留提示 (Recommended)"
**映射**：需求 A20 ✅
**落实**：Phase 1 warning 文案提及"可探索未暴露接口"，工具 Phase 4 落地

### Q8. ask 工具形态
**问题**："AI 随时 ask 用户"的实现形态？
**用户回答（custom）**：
> "新增ask工具。我们这里本质是要强化core/agent底座，D:\green_ds\DeepSeek-Reasonix是很好的借鉴，我们可以尽可能移植他们的优良实现——本项目已有足够多这样的先例。"

**映射**：需求 A11 ✅ + 工程需求 E4 ✅（Reasonix 移植优先）
**落实**：Phase 1 `core/agent/tools/ask_tool.dart`（移植 `reasonix/internal/agent/ask.go`）
**关键语义**：**强化 core/agent 底座**是目标（不是 scraper 局部工具）；**移植优先**（DeepSeek-Reasonix 为参考，已有先例）。

---

## 第 5 轮 ask（6 问 · Guardian / 回退 / done 语义 / 多画板 / 打断 / 门语义）

### Q1. Guardian 取舍
**问题**：Reasonix 的 guardian（AI 子代理审查工具调用）是否纳入？
**用户回答（选中）**："同时移植 AI Guardian"
**映射**：需求 A12 ✅
**落实**：Phase 3 `core/agent/guardian/guardian.dart`（移植 `reasonix/internal/guardian/guardian.go`）
**注意**：与 Q6 门语义结合 = 审查 trace + 产物，非逐工具调用审查。

### Q2. 回退粒度
**问题**："AI 无条件回退到已完成步骤"的粒度？
**用户回答（选中）**："阶段级回退 (Recommended)"
**映射**：需求 A14 ✅
**落实**：Phase 1 `rollbackTo(ScraperPhase)` + `rollbackHistory`

### Q3. done 后循环
**问题**：done（MVP 完成）之后的行为语义？
**用户回答（选中）**："done 可被反馈重新激活 (Recommended)"
**映射**：需求 A16 ✅
**落实**：Phase 1（done 非终态，反馈 → refining）

### Q4. 快照数量与多画板
**问题**：日志快照的存储策略？
**用户回答（custom）**：
> "我们的爬虫插件需要做多画板支持，每个画板就是一个沙盒，是一个独立的任务，不同任务绝不交叉，同一项目日志快照单快照+产物保留即可"

**映射**：
- 多画板支持 → 需求 A21 ✅
- 每画板 = 沙盒 = 独立任务，任务绝不交叉 → A21 ✅
- 同一项目日志**单快照** + **产物保留** → A21 ✅
**落实**：Phase 2 `board/`（每画板独立 WebView/会话/快照；日志单快照；产物引用保留）

### Q5. 打断实现
**问题**：用户随时打断 AI 的实现方式？
**用户回答（选中）**："停止按钮+阶段保留 (Recommended)"
**映射**：需求 A17 ✅
**落实**：Phase 1（AI 面板「停止」按钮 → `Controller.cancel()`，阶段保留）

### Q6. 门语义
**问题**："不能不经 harness 验收就 step 到下一步"的门语义？
**用户回答（custom）**：
> "显式tool审核+另调API对当前AI的trace和产物进行审核"

**映射**：需求 A13 ✅
**落实**：Phase 3 Guardian——① **显式 tool 审核**：`guardian_review` 工具供 AI 主动调用；② **另调 API**：门禁点（G5/G6）自动调独立 LLM 会话审核"当前 AI 的 trace + 产物"
**⚠️ 偏差修正**：此前 A13 未展开"显式 tool 审核"与"另调 API"两部分各自的触发者与审核对象，需补全（见 `Scraper_V2.0_全量重构计划.md` §7.2）。

---

## 第 6 轮 ask（5 问 · 画板形态 / 模式 / Guardian 触发与粒度 / 持久化 / 重划分）

### Q1. 多画板 WebView 形态
**问题**：多画板的 WebView 形态？
**用户回答（选中）**："每画板独立 WebView (Recommended)"
**映射**：需求 A22 ✅
**落实**：Phase 2 `board_container_view.dart`（每画板独立 WebView 实例）

### Q2. 画板与模式关系
**问题**：多画板与"模式 A/B"的关系？
**用户回答（选中）**："画板内选模式 (Recommended)"
**映射**：需求 A23 ✅
**落实**：Phase 2（`ScraperBoard.mode` 字段，画板内选定向/探索）

### Q3. Guardian 触发方式
**问题**：Guardian 审核（另调 API 审 trace+产物）的触发方式？
**用户回答（选中）**："门禁自动+AI 可显式调用 (Recommended)"
**映射**：需求 A12 ✅
**落实**：Phase 3（G5/G6 门禁自动触发 + `guardian_review` 工具显式调用）

### Q4. Guardian 粒度
**问题**：Guardian 审核的粒度与成本控制？
**用户回答（选中）**："审产物+关键 trace (Recommended)"
**映射**：需求 A12 ✅
**落实**：Phase 3（审核输入 = 产物 + 关键 trace 摘要，单次 LLM 调用）

### Q5. 画板持久化 + 重划分要求
**问题**：多画板的持久化策略？
**用户回答（custom）**：
> "重启恢复画板。另外，我发现当前的Phase 1其实很大，而且很多设计严重依赖Phase 2~4。这说明我们最开始的4个phase的设计就没有严格按照拓扑顺序，现在我提议根据我们讨论后的充分的信息，重新划分4个需求全满足的phase划分，重写一份new Scraper优化计划"

**映射**：
- 重启恢复画板 → 需求 A24 ✅
- **按拓扑顺序重划分 Phase**（旧划分 Phase 1 依赖 2~4 是错误）→ 需求 E2 ✅
- 重写新 Scraper 优化计划 → 本文档 + `Scraper_V2.0_全量重构计划.md`
**落实**：Phase 2 `board_store.dart`（checkpoint 式原子持久化）；§三 拓扑依赖图

---

## 转述偏差汇总（此前需求清单 vs 本归档）

| # | 此前遗漏/偏差 | 修正 |
|---|---|---|
| 1 | 未记录"A8 动机：不想要设置里出现一大堆 SCRAPER" | 补入 A8 注释 |
| 2 | 未记录"A16 前置语义：第二轮是 MVP 已满足后的反馈驱动 debug/优化" | 补入 A16 |
| 3 | 未记录"A17 的'整个 workflow 不变'（打断不改 workflow 结构）" | 补入 A17 |
| 4 | **未记录"AI 随时可调用工具阅读日志快照"** | **新增 A26：`read_request_snapshot` 工具** |
| 5 | A18 未写清"确认框可由 AI ask 触发重抓" | 修正 A18 |
| 6 | 未记录 A15 动机"调试轮次 = 防 AI 自循环的 guard" | 补入 A15 |
| 7 | A13 未展开"显式 tool 审核"与"另调 API"两部分 | 补全 §7.2 |
| 8 | 未显式记录 D9"两种模式有不同工作流 + 不同 harness 约束" | 补入 D9 |
