# 开发中功能 — 2026-06

> 以下功能处于开发/暂停状态，未经过完整测试，默认不向用户开放。
> 底层代码完整保留，仅供开发者预览。

---

## 自动签到

- **状态**: ⚠️ 开发中
- **入口**: 侧边栏「自动签到(开发中)」（已从 UI 移除）
- **说明**: 前端页面已标记为 WIP，底层 `AutosignScreen` + `AutosignService` 代码完整保留
- **阻塞**: 未经过测试，不开放

## 一卡通

- **状态**: ⏸ 暂停
- **入口**: 侧边栏「一卡通(开发中)」（已从 UI 移除）
- **阻塞**: BlueWare (慧新E校) 平台认证待实现，`synjones-auth: bearer <token>` 获取流程未自动化
- **参考**: `docs/dev/ecard-auth-notes.md`

## 图书馆

- **状态**: ⏸ 暂停
- **入口**: 侧边栏「图书馆(开发中)」（已从 UI 移除）
- **阻塞**: `api.lib.zju.edu.cn` HTTPS 连接超时，API 端点可能已变更

## RVPN

- **状态**: ⏸ 暂停
- **入口**: 侧边栏「RVPN(开发中)」（已从 UI 移除）
- **阻塞**: 需 zju-connect Go 二进制文件，不良集成

## 智能调度

- **状态**: ⏸ 暂停
- **入口**: 侧边栏「智能调度(开发中)」（已从 UI 移除）
- **说明**: Flow 调度算法已实现（`flow_scheduler.dart`），UI 未完成

## 背词

- **状态**: ⚠️ 半成品
- **入口**: 侧边栏「背词(开发中)」（已从 UI 移除）
- **说明**: FSRS 间隔重复、AI 词源分析、本地词库均已实现，打字交互模式可用
- **问题**: 未完成与新架构的完整集成测试

## 答题

- **状态**: ⏸ 暂停
- **入口**: 侧边栏「答题(开发中)」（已从 UI 移除）
- **阻塞**: classrooms API 已废弃

## 钉钉 Webhook

- **状态**: ⚠️ 开发中
- **入口**: 设置 → 其他（已从 UI 移除）
- **阻塞**: 未经过完整测试，设置界面中隐藏

## DeepSeek Vision / HuggingFace OCR

- **状态**: ❌ 废弃
- **历史**: 尝试过 DeepSeek Chat Vision API（不支持）、HuggingFace InferenceClient + DeepSeek-OCR-2（需 HF Token + 镜像）
- **当前方案**: 回退到本地 Tesseract OCR（`--psm 6` 纯文本模式）
- **脚本**: `scripts/ocr_slides.py`、`scripts/ocr_file.py`

## PDF 翻译

- **状态**: ✅ 桌面端完成 / 🟡 Android 可编译无功能承诺
- **入口**: 侧边栏「PDF 翻译」（AI 工具区）
- **说明**: 通过 Python 子进程调用 pdf2zh 引擎，输出保留排版的双语对照 PDF。首次使用自动检测 Python 并安装依赖。批量翻译 + 历史记录。
- **已知修复**: `_healLegacyPrefs()` 启动时修复旧版 SharedPreferences 类型残留；配置五层同步。
- **Android**: 标记 (开发中)，显示占位卡片（需 Python 环境，移动端不承诺功能）
- **脚本**: `scripts/pdf_translate.py`、`scripts/pdf2zh_next/`（精简版，仅核心）

---

*最后更新: 2026-06-21*
