# 我做了的事情（2026-07-10）

## 完成

1.V3 渲染器六层架构重构 🏗️
2.插件层全面清理 🧹
3.爬虫模块完整落地 🕷️
4.基础设施增强

### Bug 修复清单

1. **标记：html 渲染函数按组件域拆分** — 38 个 render*.dart 的 import 路径从 `../../html/html_helpers.dart` → `../shared/html_helpers.dart`
2. **video_player → media_kit 迁移** — 修复 Windows 桌面无 `video_player` 平台实现的问题
3. **dock 布局 P0 封面页 top/bottom 不显示** — `LayoutPreset.fromJson` 增加从嵌套 `regions` 提取的逻辑
4. **markdown 组件未解析渲染** — `MarkdownSlot` 从纯 `Text` 改为 `MarkdownRenderer` 管线
5. **数据仪表盘 P1 6 组件空白** — 创建/重写 stat-tile / chart / card-list / kanban / timeline / data-table 6 个 slot，从 config 读取真实数据
6. **grid 布局自适应行高** — `SizedBox(height)` → `ConstrainedBox(minHeight)`，DashboardCard 紧凑化
7. **data-table 双重 Expanded 陷阱** — 修复语法错误 + 替换为 `ListView`（独立 ScrollController）
8. **Scrollbar + ListView(primary:true) 多 slot 冲突** — 每个 slot 持有独立 `ScrollController`
9. **P2 MindmapSlot overflow** — `Column(min)` → `Column(max)` + `Expanded`
10. **Spreadsheet/Terminal 占位修复** — 真数据渲染替代 TODO 占位
11. **xterm + flutter_pty 真终端** — 替代 mock terminal，真实 shell 交互
12. **scraper 3 个编译错误** — `request_log_panel` 缺少 context、`Controller.run` → `send`
13. **scraper 侧边栏不显示** — icon `bug_report` 不在 `_iconMap` 中，改为 `language`
14. **scraper WebView 从 webview_flutter → webview_windows** — Windows 无 `WebViewPlatform` 实现
15. **scraper AI 工作区 markdown 渲染** — 助手消息从纯 `Text` 改为 `MarkdownRenderer`
16. **scraper 请求日志不完整** — 三层捕获替代仅 fetch/XHR
17. **main.dart 2 个编译错误** — `additionalArguments` 类型 + `setMockInitialValues` 测试标记
18. **cdp_network_client 编译错误** — `StringBuffer?` 类型提升
19. **scraper 导出空白** — `exportAsPython` 增加从文件回退 + 模板注入检测
20. **scraper AI 不遵守凭证写入流程** — Skill Step 2 重写为硬性约束
21. **其他未被记录的bug**

## 当前规划

- [ ] `showcase-v3` 全量组件修复（Dart + HTML 双端）
- [ ] 部署 `M2规划.md` 并推进。

---

by 绿意不息
