# v2.0 重点优化对象（基于代码实况盘点）

> 本表所有结论均来自 `evg-base/` 下真实代码，未实现项注明"无代码证据"而非猜测。实现程度只陈述代码中"存在且被调用"的部分。

## 表一：四个插件真实实现程度与功能盘点

| 插件 | manifest 宣称 | 真实已实现（有代码证据） | 未实现 / 未验证 | 总评 |
|---|---|---|---|---|
| **ai-assistant** | 全功能聊天、深度思考、联网搜索、工具调用、多会话管理；workspace 50 文件/50MB/aiCreatable | ① 完整聊天 UI：气泡/流式光标/thinking 块/tool_call 卡片（`lib/renderer/templates/v4_modle/components/interaction/chat/messages/`）。② 真实流式：SSE 解析 `reasoning_content`/`content`/`tool_calls`（`lib/core/agent/provider.dart:220-428`）。③ 深度思考：`reasoningEffortProvider` 四档 → `thinking.reasoning_effort`（`lib/core/agent/agent_runtime.dart:150-169`）。④ 联网搜索/抓取：`WebSearchTool`(Bing)+`WebFetchTool`（`lib/core/agent/tools/web_search.dart`）。⑤ 工具调用：read/write/grep/head-tail/file-info/run_skill/read-write-global-memory/python_runner（`agent_runtime.dart:83-111`）。⑥ 多会话：`session_manager.dart` + `file_session_store.dart`。⑦ 上下文压实：`Compactor`（`lib/core/agent/compact/compact.dart`）。⑧ 全局记忆：`FileMemoryStore`+`MemoryAgent`（`agent_runtime.dart:59,123`） | **RAG/本地知识库**：全仓库无 embedding/vector/faiss/chroma 检索代码。**workspace 的 maxFiles/maxSizeMb/aiCreatable 强制校验**：manifest 声明存在，`core/module/module_descriptor.dart` 解析该字段，但"上传文件数/大小上限"与"AI 创建文件"的强制执行路径未见接入聊天流证据。 | 对话与 Agent 能力已相当完整；**RAG 是真正的空白项**，workspace 配额落地存疑 |
| **theme-creator** | 8 色语义色板 + Dart 实时预览 + AI 生成 + 一键导出 | ① 8 色模型：`background/surface/border/text/textSecondary/accent/error/others`（`lib/core/theme/theme_descriptor.dart`；AI prompt 同构 `theme_creator_modle/services/theme_ai_service.dart:45-62`）。② AI 生成：`ThemeAiService.generate` 真实调用 `DeepSeekProvider`（`theme_ai_service.dart:30-84`）。③ Dart 实时预览：真实 `MaterialApp`+`buildAppThemeFromDescriptor`（`widgets/theme_preview.dart:24-35`）。④ 一键导出：写 `plugins/<id>/theme/theme.json` + `ThemeStore.register` 热注册（`services/theme_exporter.dart:38-90`）。⑤ 校验：id 合法性/8 色齐全/hex 合法（`theme_exporter.dart:38-58`；`core/theme/test/token_validation_test.dart`）。⑥ 草稿持久化：JSON 落盘（`services/theme_draft_store.dart`） | **主题模板库 + 一键应用**：无模板库代码；`builtin_themes.dart` 是内置 dark/light 等预设，不是创作者可选择的"模板库" | 闭环完整（编辑→AI→预览→校验→导出），**只缺模板库** |
| **scraper** | WYSIWYG 抓包 + AI 生成 Python + 导出 .py/.exe | ① 内嵌浏览器：Windows WebView2 + Android webview_flutter 双端（`scraper_modle/scraper_webview.dart:209-248`）。② 抓包：CDP `--remote-debugging-port=9222` 注入 + JS 三层拦截（PerformanceObserver/fetch/XHR）（`scraper_webview.dart:34-167,838-853`；`cdp_network_client.dart`）。③ AI 生成：隔离 Agent + 爬虫专用工具 + skill（`scraper_ai_panel.dart:1-80`）。④ 导出 .py：`exportAsPython` 强制注入锁定配置模板（`scraper_exporter.dart:131-210`）。⑤ 导出 .exe：PyInstaller `--onefile`（`scraper_exporter.dart:300-396`）。⑥ 终端执行：真实 `Process.run` Python（`scraper_terminal.dart`）。⑦ 工作流状态机（`scraper_workflow.dart`） | **WYSIWYG 点选元素生成选择器**：代码是"浏览+抓包"，非点击页面元素产出 selector（grep selector/clickElement 无实现）。**反爬机制（UA 轮换/代理池/分布式）**：仅存在于 AI skill 提示词（`scraper_skill.md`），无运行时代码。 | 抓包→AI 生成→执行→导出链路真实完整；**"点选元素"与"反爬机制"是宣传与实现的差距** |
| **html-creator** | 三栏 IDE + 实时预览 + AI 生成 + 一键导出 | ① 三栏布局：`HtmlCreatorView` + `view/editor_panel.dart`(多标签 HTML/CSS/JS)+`view/data_panel.dart`+`view/preview_panel.dart`（`v4_modle/components/creative/html-creator/`）。② 实时预览：WebView + HttpServer + CSS zoom，Desktop/Tablet/Mobile 三档（`view/preview_panel.dart:21-60`）。③ AI 生成：`HtmlAiService` 真实 Agent + 专用工具 `write_html_file/read_html_file/export_html_plugin`（`services/html_ai_service.dart:1-31`；`services/html_creator_tools.dart`）。④ 一键导出：双目录写 `module/manifest.json` + `index.html`（`services/html_export_service.dart:40-135`）。⑤ `platform.*` 桥接：`html_modle/bridge_script.dart` + `core_api_discovery.dart` 真实注入。⑥ 数据中枢：`data_preview_service.dart` 读数据源快照 | **热重载调试**：无 hot-reload 代码。**"常用 UI 组件封装（表格/表单/图表）"**：未见组件库封装，依赖用户手写 HTML。**编辑器是 TextField 多标签**，非语法高亮/补全的代码编辑器 | 创作闭环完整；**缺热重载，编辑器与组件库是工程化短板** |

---

## 表二：工作流化 / 工程化提升生成质量策略

| 插件 | 目标 | 策略（落地指向，均基于现有代码缺陷） | 依赖 / 优先级 |
|---|---|---|---|
| **ai-assistant** | 接入本地知识库，把"工具调用"升级为"检索增强" | ① 复用现有 `FileMemoryStore`/`MemoryAgent`（`agent_runtime.dart:59,123`），把"全局记忆"扩展为**可检索知识库**：新增 embedding/检索 store，而非另起炉灶。② 在 `Provider.chat` 前插入"检索→注入 system prompt"管线，与 `Compactor` 统一 token 预算（`compact/compact.dart`）。③ 将 workspace 的 `maxFiles/maxSizeMb/aiCreatable` 接入 `agent/tools/workspace_tool.dart` 与 `write_file.dart`，补上配额强制校验，使 manifest 声明真实生效 | 高（RAG 是最大功能缺口） |
| **theme-creator** | 模板库化，提高生成主题的复用率与一致性 | ① 在 `ThemeDraftStore`（`theme_creator_modle/services/theme_draft_store.dart`）外新增 `TemplateStore`，复用同一 JSON 格式。② 把 `core/theme/builtin_themes.dart` 的预设主题迁移为"模板库"数据源，UI 提供一键应用。③ AI 生成结果落库前强制走 `ThemeExporter` 同一套校验（`theme_exporter.dart:38-58`），保证"AI 生成→模板→导出"三者共享校验契约 | 中（闭环已通，属增强） |
| **scraper** | 补齐"点选元素"与"反爬"，把 demo 级工具工作流化 | ① 基于现有 WebView+JS 通道（`scraper_webview.dart:34-167`），注入点击监听产出 **CSS selector/XPath**，把"浏览抓包"升级为"点选→选择器→AI 生成"（消解 WYSIWYG 名实不符）。② 把 `scraper_skill.md` 中描述的 UA 轮换/代理池从"提示词"下沉为 `scraper_template.dart` 里的**运行时库代码**（不可被 AI 改动的锁定段，与 `scraperConfigTemplate` 同机制）。③ 用 `ScraperWorkflow` 状态机（`scraper_workflow.dart`）为每个阶段定义验收门槛（请求捕获→schema 推断→代码生成→执行通过），形成可复现流水线 | 高（点选是宣传核心） |
| **html-creator** | 提升插件生成质量：组件库 + 语法编辑 + 热重载 | ① 把导出产物（`module/manifest.json`+`index.html`，`html_export_service.dart:97-106`）抽象为**模板脚手架 + 组件片段库**，AI 只填数据绑定，减少手写 HTML 出错。② 用 `TextEditingController`（现 `editor_panel.dart:6-9`）替换为带语法高亮/错误提示的编辑组件。③ 基于 `PreviewPanel` 现有 WebView+HttpServer 能力（`preview_panel.dart:52-60`），增加文件变更监听 → 自动刷新，补齐热重载。④ 把 `core_api_discovery.dart` 暴露的 `platform.*` API 固化进 AI prompt，让 AI 生成的 HTML 默认调用平台契约而非自定义全局变量 | 高（编辑器/组件库/热重载是生成质量瓶颈） |

---

> 说明：优先级标注基于"宣称与实现的差距大小"与"对生成质量的杠杆效应"。凡上表标记"无代码证据"项，均为本次盘点中未能在 `evg-base/` 找到实现的宣称。