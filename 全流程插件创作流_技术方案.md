# 全流程插件创作流 — 可执行技术方案

> **版本**: 1.0 | **创建**: 2026-07-11 | **状态**: 待评审
> **目标**: 构建从数据采集到插件上架的全自动化创作流水线

---

## 一、项目概述

**一句话定位**: Evergreen 全流程插件创作流——让用户通过「所见即所得爬虫 → 配置向导 → PPT 式编排 → 实时预览」四步，零代码生成六维插件，5 分钟内从零到上架。

**核心价值**: 将插件开发门槛从「写 JSON + 编译 .exe」进一步降到「框选网页 + 拖放组件」，实现纯 GUI 的插件全生命周期管理。

---

## 二、架构设计

### 2.1 分层架构

```
用户交互层（renderer/components/）
┌────────────────────────────────────────────────────────────┐
│  ScraperGeneratorView   PluginDesignerView   PreviewPane   │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────┐ │
│  │ CDP WebView     │  │ Page/Slot/Comp │  │ Real-time    │ │
│  │ 数据捕获面板    │  │ 编排面板       │  │ Dart 预览    │ │
│  └───────┬────────┘  └───────┬────────┘  └──────┬───────┘ │
└──────────┼──────────────────┼───────────────────┼──────────┘
           │                  │                   │
编排层（services/）
┌──────────┼──────────────────┼───────────────────┼──────────┐
│  ┌───────▼────────┐  ┌──────▼───────┐  ┌───────▼───────┐ │
│  │ PluginGenerator │─▶│ Manifest    │─▶│ PluginRegistry │ │
│  │ (意图→插件)     │  │ Assembler   │  │ (文件热加载)   │ │
│  └───────┬────────┘  └──────┬───────┘  └───────┬───────┘ │
└──────────┼──────────────────┼───────────────────┼──────────┘
           │                  │                   │
基础设施层（core/）
┌──────────┼──────────────────┼───────────────────┼──────────┐
│  ┌───────▼────────┐  ┌──────▼───────┐  ┌───────▼───────┐ │
│  │ DataHttpServer  │  │ConfigHttp    │  │ ModuleLoader  │ │
│  │ (数据管线注册)  │  │Server(配置)  │  │ (.exe进程管理)│ │
│  └────────────────┘  └──────────────┘  └───────────────┘ │
└────────────────────────────────────────────────────────────┘
```

### 2.2 模块划分

| 模块 | 目录 | 职责 | 依赖 |
|------|------|------|------|
| **ScraperFlow** | `.../document/scraper/` | CDP 抓包 → 解析 → 生成 data 插件 | webview_windows, CDP |
| **DesignerFlow** | `.../document/plugin-designer/` | PPT 式 Page/Slot/Component 编排 | none |
| **PluginGenerator** | `.../services/plugin_generator.dart` | 意图 → manifest.json + .py + .exe | agent, data, config |
| **PluginPreloader** | `.../services/plugin_preloader.dart` | 文件监控 → 热加载 → 预览同步 | ModuleLoader |
| **DataPluginer** | `.../services/data_pluginer.dart` | 爬虫输出 → DataSource manifest | scraper, data |

### 2.3 依赖关系

```
ScraperFlow ──→ DataPluginer ──→ PluginGenerator ──→ PluginPreloader
                                                       │
DesignerFlow ──────────────────────────────────────────┘
                                                       │
                                                       ▼
                                              ModuleLoader.scanModules()
```

---

## 三、技术选型

| 技术 | 版本 | 选择理由 | 仓库状态 |
|------|------|---------|---------|
| Flutter | 3.x (Dart 3.x) | 跨平台桌面，热重载基础 | ✅ 已有 |
| webview_windows | ^3.x | WebView2 CDP 远程调试端口 9222 | ✅ 已有依赖 |
| dart:io (Process) | 标准库 | 启动/管理 .exe 子进程 | ✅ 已有 ProcessManager |
| Agent Runtime | 内部 | 意图理解 → 代码生成 | ✅ 已有 |
| lxml / beautifulsoup4 | 最新 | Python 端 HTML 解析 | ✅ scripts/ 中已有 |
| PyInstaller | ^6.21 | Python 脚本 → 单文件 .exe | ✅ 已有 |
| watchman / dart:io | — | 文件监控 → 热加载触发器 | 新增 |

**新增依赖**:

| 依赖 | 用途 | 类别 |
|------|------|------|
| `watcher` (Dart pub) | 监控 plugins/ 目录文件变化 | pubspec.yaml 新增 |
| `pathspec` (Dart pub) | 过滤不需要触发热更新的文件 | pubspec.yaml 新增 |

---

## 四、核心模块

### 4.1 ScraperFlow — 所见即所得数据采集

**职责**: 内嵌浏览器中标选目标数据 → 自动生成 Python 爬虫 + 注册 data 插件

**关键文件**:

| 文件 | 职责 |
|------|------|
| `scraper/scraper_webview.dart` | WebView2 内嵌浏览器，CDP Network 域捕获 |
| `scraper/scraper_ai_panel.dart` | AI 分析页面 → 识别数据规律 |
| `scraper/scraper_exporter.dart` | 导出为 data source .py + manifest.json |
| `scraper/scraper_tools.dart` | DOM 选择器、XPath 辅助工具栏 |
| `scraper/scraper_workflow.dart` | 四阶段状态机 |

**对外接口**:

```dart
class ScraperFlow {
  /// 开始捕获目标 URL 的 HTTP 请求
  Future<void> startCapture(String url);

  /// 停止捕获，返回捕获到的请求列表
  Future<List<CapturedRequest>> stopCapture();

  /// AI 分析选中的请求，推断数据结构
  Future<InferredSchema> analyzeSelection(List<CapturedRequest> requests);

  /// 生成 data 插件包（.py + manifest.json）
  Future<GeneratedPlugin> generateDataPlugin(InferredSchema schema);
}
```

**流程**:

```
用户输入 URL → WebView2 加载 → CDP Network.enable → 实时显示请求日志
                ↓
用户选中目标请求 → 点击"AI 分析" → 推断数据字段和类型
                ↓
AI 生成 Python 爬虫脚本（含 lxml 解析逻辑）+ data manifest.json
                ↓
点击"注册插件" → 写入 plugins/<name>/data/ → 热加载生效
```

### 4.2 DataPluginer — 数据插件自动注册

**职责**: 将 ScraperFlow 输出的爬虫脚本包装为标准 data 插件

**关键文件**:

| 文件 | 职责 |
|------|------|
| `services/data_pluginer.dart` | data manifest 生成 + 端口注册 |
| `services/data_pluginer_skill.dart` | AI 生成 data 插件的 Skill prompt |

**data manifest 模板**:

```json
{
  "schemaVersion": "2.0",
  "type": "data-source",
  "id": "custom-{name}",
  "name": "{name}",
  "endpoint": "/data/{name}",
  "refreshInterval": 3600,
  "process": [
    {
      "exe": "data/{name}.exe",
      "protocol": "http"
    }
  ]
}
```

### 4.3 ConfigRegister — 配置自动注册

**职责**: 分析 data 插件输出字段 → 自动生成 config.json

**关键文件**:

| 文件 | 职责 |
|------|------|
| `services/config_register.dart` | 配置项推断 + config.json 生成 |
| `services/config_register_skill.dart` | AI 分析数据字段 → 配置参数映射 |

**流程**:

```
从 data 插件的 manifest + .py 中提取:
  ├── 请求参数（URL、headers、cookies）→ config.credentials
  ├── 输出字段 → config.displayOptions
  └── 定时策略 → config.schedule
                  ↓
生成 plugins/<name>/config/config.json
                  ↓
ConfigHttpServer 自动检测 → 动态注册设置项
```

### 4.4 DesignerFlow — PPT 式编排编辑器

**职责**: 可视化编排 plugin 的 pages → slots → components 层次，像 PPT 一样"画"出界面

**关键文件**（新增目录 `.../document/plugin-designer/`）:

| 文件 | 职责 |
|------|------|
| `plugin_designer_view.dart` | 主视图（三栏: 组件面板 | 画布 | 属性编辑） |
| `view/canvas_area.dart` | 拖放画布，支持框选区域划定 Slot |
| `view/page_sorter.dart` | 页面缩略图排序（类似 PPT 左侧缩略图栏） |
| `view/slot_painter.dart` | 在画布上绘制 Slot 区域框 |
| `view/component_picker.dart` | 可选组件面板（按 7 功能域分组） |
| `view/property_panel.dart` | 选中组件/槽位的属性编辑面板 |
| `models/design_document.dart` | 编排文档模型（pages → slots → components） |
| `services/design_to_manifest.dart` | 设计文档 → manifest.json 编译器 |
| `services/design_doc_service.dart` | 设计文档持久化服务 |

**编排模型**:

```
DesignDocument
  ├── pages[]: DesignPage
  │     ├── id, label, layoutPreset
  │     └── slots[]: DesignSlot
  │           ├── id, region (top/left/center/right/bottom)
  │           ├── rect (在画布中的像素坐标 + 宽高)
  │           └── component: DesignComponent
  │                 ├── type (如 "data-table", "chart")
  │                 └── config (字段级属性)
  └── metadata
        ├── pluginId, pluginName
        └── dependencies[]
```

**交互流程**:

```
┌─ 左侧面板 ─┬─ 中间画布 ──────────────┬─ 右侧面板 ─┐
│ [组件库]    │                        │ [属性编辑]  │
│             │                        │             │
│  📊 数据     │  ┌────────────────┐    │ 类型: chart │
│  data-table │  │ 顶部区域 (Slot) │    │ 标题: ___   │
│  chart      │  ├────────────────┤    │ 类型: bar   │
│  card-list  │  │ 中间区域        │    │ 数据: [...] │
│  ...        │  │ (用户框选的Slot)│    │             │
│             │  └────────────────┘    │             │
│  📝 文档     │                        │             │
│  markdown   │ [画布操作]              │             │
│  document   │ 框选=创建Slot          │             │
│  ...        │ 点击Slot=选中          │             │
│             │ 拖入组件=绑定           │             │
│             │ 拖拽调整布局            │             │
└─────────────┴────────────────────────┴─────────────┘
```

**四个编排阶段**（用户逐层进行）:

| 阶段 | 操作 | 产出物 |
|------|------|--------|
| **① Module 配置** | 输入插件名称、图标、路由、描述 | 基础 manifest 头信息 |
| **② 页面编排** | 添加/删除/排序页面，选择布局范式（dock/grid/flex/fullscreen） | designDocument.pages[] |
| **③ Slot 框选** | 在画布上拖拽框选矩形区域 → 自动创建 Slot（命名+定位） | designDocument.pages[].slots[] |
| **④ 组件绑定** | 从组件面板拖入组件类型到 Slot → 编辑属性 | designDocument.pages[].slots[].component |

### 4.5 Flutter Sync Renderer — 实时热预览

**职责**: 插件选择/修改后，立即在 plugins/ 目录同步文件，触发 ModuleLoader 热加载，并在预览面板中展示

**关键文件**:

| 文件 | 职责 |
|------|------|
| `services/plugin_preloader.dart` | 文件监控 + ModuleLoader 热加载触发 |
| `services/preview_sync_service.dart` | 编排变更 → manifest 写入 → 预览刷新 |

**同步链路**:

```
DesignerFlow 中用户操作
        ↓
design_document 更新
        ↓
DesignToManifest.compiler(designDoc)
        ↓
write_file → plugins/<id>/module/manifest.json
        ↓
PluginPreloader.watch() 检测到文件变化
        ↓
ModuleLoader.scanModule('plugins/<id>/module/')
        ↓
ModuleRegistry.reregister(moduleDescriptor)
        ↓
PreviewPanel 监听 registry → 重建页面
```

**热加载策略**:

| 变更类型 | 操作 | 预热时间 |
|---------|------|---------|
| manifest 字段修改 | 重新 parse + 更新 registry 条目 | < 100ms |
| 新增 Slot | 重建 composite_view + 重新布局 | < 200ms |
| 替换组件类型 | Slot 内替换 widget | < 50ms |
| .exe 进程变化 | 重启子进程 | < 1s |
| 全量重建 | 重新 scanModule + seal | < 500ms |

### 4.6 完整编排视图组件树

```
renderer/components/document/plugin-designer/
├── plugin_designer.dart                    # barrel
├── plugin_designer_view.dart              # 主视图
├── plugin_designer_slot.dart              # SlotDispatch 注册
├── view/
│   ├── canvas_area.dart                   # 拖放画布
│   ├── page_sorter.dart                   # 页面排序栏
│   ├── slot_painter.dart                  # Slot 绘制器
│   ├── component_picker.dart              # 组件选择面板
│   └── property_panel.dart               # 属性编辑面板
├── models/
│   ├── design_document.dart               # 编排文档模型
│   ├── design_page.dart                   # 页面模型
│   ├── design_slot.dart                   # Slot 模型
│   └── design_component.dart              # 组件配置模型
├── services/
│   ├── design_to_manifest.dart            # 设计→manifest 编译
│   ├── design_doc_service.dart            # 设计文档持久化
│   ├── plugin_preloader.dart              # 文件监控+热加载
│   └── preview_sync_service.dart          # 预览同步
├── widgets/
│   ├── slot_frame.dart                    # 画布上的 Slot 框
│   ├── draggable_component.dart           # 可拖拽组件
│   ├── property_editor.dart               # 通用属性编辑器
│   └── plugin_icon_picker.dart            # 图标选择器
└── test/
    ├── design_document_test.dart
    ├── design_to_manifest_test.dart
    └── plugin_preloader_test.dart
```

---

## 五、数据流

### 5.1 全流程数据流（核心场景）

```
用户操作                   系统动作                    数据变化
──────────────────────────────────────────────────────────────────
① 打开爬虫 → WebView2         CDP 连接 WebView2
   输入 URL                       → Network.enable
                                  → 开始捕获请求日志
                                                        捕获请求队列 []

② 选中请求 → AI 分析            Agent 调 web_search
   推断数据结构                   分析请求响应体
                                  → 推断字段类型
                                                        推断的 Schema

③ 点击"生成插件"               PluginGenerator:
                                  → 生成 .py 爬虫
                                  → 生成 data manifest.json
                                  → 生成 config.json
                                  → 写入 plugins/<name>/data/
                                  → 写入 plugins/<name>/config/
                                                        磁盘文件

④ 自动热加载                   ModuleLoader 检测到新目录
                                  → 启动 .exe 进程
                                  → 注册 data source
                                  → config 注册设置项
                                                        ModuleRegistry 更新

⑤ 切换到 DesignerFlow          读取 data/data 绑定
   编排页面/Slot/组件             → 填充组件选项
   框选 Slot                      → 生成 designDocument
   拖入组件                       → 实时预览
                                                        当前设计文档

⑥ 点击"保存插件"               DesignToManifest 编译
                                  → 写入完整 manifest.json
                                  → 写入 plugins/<name>/
                                  → PluginPreloader 检测
                                  → 热加载生效
                                                        完整插件目录

⑦ 预览面板实时展示               ModuleDispatch 重建
                                  → CompositeView 解析
                                  → 组件树渲染
                                                        最终 UI
```

### 5.2 状态变化

| 阶段 | 用户状态 | 系统状态 | 持久化 |
|------|---------|---------|--------|
| 爬虫 | `CapturePhase` | CDP 连接中，请求日志增长 | session 缓存 |
| 分析 | `AnalyzePhase` | Agent 推理中 | 推断字段 JSON |
| 生成 | `GeneratePhase` | 文件写入中 | plugins/ 目录 |
| 编排 | `DesignPhase` | 画布交互/Slot 框选 | design_doc JSON |
| 热预览 | `PreviewPhase` | 监听文件变化 | 实时重建 UI |
| 发布 | `PublishPhase` | 完整插件包就绪 | 完整 plugins/ |

---

## 六、实现路径

### P0 — 基础设施（3 天）

| 任务 | 文件 | 验收标准 |
|------|------|---------|
| 新增 `watcher` 依赖 | `pubspec.yaml` | `flutter pub get` 通过 |
| 实现 `PluginPreloader` | `services/plugin_preloader.dart` | 监控 plugins/ 目录变化并触发事件 |
| 实现 `PreviewSyncService` | `services/preview_sync_service.dart` | 文件修改 → 预览刷新回调 |
| 创建 plugin-designer 目录骨架 | 全部新目录 + barrel 文件 | `dart analyze` 零错误 |
| 实现 `DesignDocument` 模型 | `models/design_document.dart` | 序列化/反序列化测试通过 |

### P1 — 爬虫到数据插件闭环（5 天）

| 任务 | 文件 | 验收标准 |
|------|------|---------|
| 完善 `ScraperExporter` | `scraper/scraper_exporter.dart` | 生成标准 data manifest.json |
| 实现 `DataPluginer` | `services/data_pluginer.dart` | 爬虫输出 → data 插件注册 |
| 实现 `ConfigRegister` | `services/config_register.dart` | data 插件字段 → config.json |
| 实现 `PluginGenerator` | `services/plugin_generator.dart` | 完整三步生成 + 写入 plugins/ |
| 端到端测试 | `test/scraper_to_plugin_test.dart` | 输入 URL → 产出可注册插件 |

### P2 — PPT 式编排核心（7 天）

| 任务 | 文件 | 验收标准 |
|------|------|---------|
| 实现 `PluginDesignerView` 主视图 | `plugin_designer_view.dart` | 三栏布局渲染 |
| 实现 `CanvasArea` 画布 | `view/canvas_area.dart` | 支持拖拽框选 Slot |
| 实现 `SlotPainter` | `view/slot_painter.dart` | 框选区域显示虚线框 + 命名标签 |
| 实现 `ComponentPicker` | `view/component_picker.dart` | 按域分组显示 65 种组件图标+名称（45 具名 + 20 placeholder） |
| 实现 `PropertyPanel` | `view/property_panel.dart` | 选中组件后显示可编辑字段 |
| 实现 `PageSorter` | `view/page_sorter.dart` | 页面缩略图排序 + 增删 |
| 实现 4 阶段编排 | (集成) | ①→②→③→④ 逐步推进 |
| 实现 `DesignToManifest` 编译器 | `services/design_to_manifest.dart` | design doc → 标准 manifest.json |

### P3 — 热加载实时预览（4 天）

| 任务 | 文件 | 验收标准 |
|------|------|---------|
| 集成 `PluginPreloader` + `DesignerFlow` | `services/preview_sync_service.dart` | 编排变更 → manifest 写入 → 热加载 |
| 实现 PreviewPanel 组件 | 新 `preview_panel.dart` | 分屏显示编排结果 |
| 实现 `.exe` 自动编译 | `services/auto_compile_service.dart` | .py 修改 → PyInstaller 编译 → 进程重启 |
| 热加载性能优化 | `services/preview_sync_service.dart` | manifest 字段变更 < 100ms 反射 |

### P4 — 发布与市场集成（3 天）

| 任务 | 文件 | 验收标准 |
|------|------|---------|
| "发布到市场"按钮 | `plugin_designer_view.dart` | 插件包 ZIP → marketplace 目录 |
| 插件元数据编辑器 | `view/property_panel.dart` | 图标、描述、版本号编辑 |
| 导出功能 | `services/design_to_manifest.dart` | 导出完整插件目录 ZIP |
| 用户引导流程 | `view/onboarding_overlay.dart` | 首次使用引导蒙层 |

---

## 七、风险与对策

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| CDP 连接不稳定 | 中 | 高 — 无法捕获请求 | 降级为手动粘贴 cURL；重试机制 |
| AI 生成代码有语法错误 | 高 | 中 — .exe 编译失败 | 生成后自动 `dart analyze`/`py_compile` 校验 |
| 热加载导致 UI 闪烁 | 中 | 低 — 用户体验下降 | 防抖 300ms + 过渡动画 |
| ModuleRegistry 不可变性冲突 | 低 | 高 — 需要 seal 后才能查询 | 热加载模块走独立 Registry（不 seal） |
| WebView2 在某些环境下不可用 | 中 | 高 — 爬虫流程不可用 | 降级为纯 HTTP 请求模式 + 用户手动输入 |
| 编排产生的 manifest 与 SchemaVersion 不兼容 | 低 | 高 — 插件无法加载 | DesignToManifest 编译器始终输出当前支持的 schema |
| 多个 Slot 重叠/布局冲突 | 中 | 中 — 画布混乱 | 碰撞检测 + 吸附网格 |

---

## 八、部署方案

### 8.1 构建流程

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ flutter test  │───▶│ dart analyze  │───▶│ flutter build │
│ 全部通过      │    │ 零错误       │    │ windows --    │
│              │    │              │    │ release       │
└──────────────┘    └──────────────┘    └──────────────┘
                                              │
                                              ▼
                                       ┌──────────────┐
                                       │ evergreen_    │
                                       │ multi_tools   │
                                       │ .exe          │
                                       └──────────────┘
```

### 8.2 测试策略

| 测试层级 | 范围 | 工具 |
|---------|------|------|
| 单元测试 | 模型类、编译器、序列化 | `flutter test` |
| Widget 测试 | 画布交互、Slot 渲染、属性编辑 | `flutter test` |
| 集成测试 | 爬虫→生成→热加载全流程 | `flutter test integration_test/` |
| E2E 测试 | 用户模拟操作完整流程 | 手动验收 |

### 8.3 关键测试用例

```dart
// 画布框选测试
testWidgets('框选区域创建 DesignSlot', (tester) async {
  await tester.pumpWidget(createCanvasArea());
  // 模拟拖拽框选
  await tester.timedDrag(
    find.byType(GestureDetector),
    const Offset(200, 150), // 从(0,0)拖到(200,150)
    Duration(milliseconds: 200),
  );
  await tester.pump();
  // 验证 slot 被创建
  expect(find.byType(SlotFrame), findsOneWidget);
  expect(find.text('slot_1'), findsOneWidget);
});

// 设计→manifest 编译测试
test('DesignToManifest 生成完整 manifest JSON', () {
  final doc = DesignDocument(pages: [
    DesignPage(id: 'page_1', slots: [
      DesignSlot(id: 'slot_1', component: DesignComponent(type: 'chart', config: {'title': '测试图表'}))
    ])
  ]);
  final manifest = DesignToManifest.compile(doc);
  expect(manifest['pages'][0]['id'], 'page_1');
  expect(manifest['pages'][0]['layout']['slots']['slot_1']['component']['type'], 'chart');
});
```

### 8.4 环境要求

| 组件 | 要求 |
|------|------|
| Flutter SDK | >= 3.x (Dart 3.x) |
| Windows | WebView2 Runtime（Win10+ 内置） |
| Python | >= 3.10（用于 PyInstaller 编译 .exe） |
| 磁盘 | 100MB 可用（插件开发缓存） |

---

## 附：文件变更清单

| 动作 | 路径 | 行数估算 |
|------|------|---------|
| **新增** | `pubspec.yaml` (+watcher, +pathspec) | +2 |
| **新增** | `renderer/components/document/plugin-designer/plugin_designer.dart` | 10 |
| **新增** | `renderer/components/document/plugin-designer/plugin_designer_view.dart` | 350 |
| **新增** | `renderer/components/document/plugin-designer/plugin_designer_slot.dart` | 40 |
| **新增** | `renderer/components/document/plugin-designer/view/canvas_area.dart` | 280 |
| **新增** | `renderer/components/document/plugin-designer/view/page_sorter.dart` | 120 |
| **新增** | `renderer/components/document/plugin-designer/view/slot_painter.dart` | 150 |
| **新增** | `renderer/components/document/plugin-designer/view/component_picker.dart` | 180 |
| **新增** | `renderer/components/document/plugin-designer/view/property_panel.dart` | 220 |
| **新增** | `renderer/components/document/plugin-designer/models/design_document.dart` | 80 |
| **新增** | `renderer/components/document/plugin-designer/models/design_page.dart` | 40 |
| **新增** | `renderer/components/document/plugin-designer/models/design_slot.dart` | 50 |
| **新增** | `renderer/components/document/plugin-designer/models/design_component.dart` | 60 |
| **新增** | `renderer/components/document/plugin-designer/services/design_to_manifest.dart` | 200 |
| **新增** | `renderer/components/document/plugin-designer/services/design_doc_service.dart` | 120 |
| **新增** | `renderer/components/document/plugin-designer/services/plugin_preloader.dart` | 180 |
| **新增** | `renderer/components/document/plugin-designer/services/preview_sync_service.dart` | 150 |
| **新增** | `renderer/components/document/plugin-designer/widgets/slot_frame.dart` | 80 |
| **新增** | `renderer/components/document/plugin-designer/widgets/draggable_component.dart` | 90 |
| **新增** | `renderer/components/document/plugin-designer/widgets/property_editor.dart` | 160 |
| **新增** | `renderer/components/document/plugin-designer/widgets/plugin_icon_picker.dart` | 60 |
| **修改** | `scraper/scraper_exporter.dart`（增强 data manifest 生成） | +80 |
| **新增** | `services/data_pluginer.dart` | 120 |
| **新增** | `services/config_register.dart` | 100 |
| **新增** | `services/plugin_generator.dart` | 180 |
| **新增** | `services/auto_compile_service.dart` | 80 |
| **新增** | `test/plugin_designer_test.dart` | 200 |
| **新增** | `test/scraper_to_plugin_test.dart` | 150 |
| **新增** | `test/design_to_manifest_test.dart` | 80 |
| **修改** | `renderer/components/document/document.dart`（+export) | +2 |
| **P0-P4 合计** | **28 文件** | **~3,380 行** |
